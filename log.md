---
title: Activity Log
type: log
---

# Activity Log

> Registro cronológico de ingestões, consultas e manutenções do vault. Append-only.

---
## 2026-08-11 (cont. V, madrugada de 12/08) — 🔥 Firebase criado VIA BROWSER e push ATIVADO fim-a-fim

Dono pediu "faça tudo, eu só logo" → Playwright abriu o console do Firebase, ele logou, eu executei: projeto **whatspro-crm-8faae** (Gemini/Analytics off), app Android `br.com.wsmart.whatspro` (apelido WhatsPRO), download do `google-services.json` + chave da service account → movidos pra `mobile/` (gitignored, conferido). Secrets: `GOOGLE_SERVICES_JSON` (gh) + `FIREBASE_SERVICE_ACCOUNT` (supabase secrets, base64). **Validação servidor completa SEM depender do aparelho:** probe node local = OAuth token OK + FCM v1 respondendo 400 INVALID_ARGUMENT a token falso (**API ativa**; 403 seria desabilitada); re-disparo do cron em prod = `{ok:true,candidates:0,...}` (varredura REAL, saiu do `firebase_not_configured`). **APK v1.2-build3** (versionCode 3, UA `whatspro-app/3`, CI logou "push ATIVO", project id conferido no resources.arsc do APK) → `C:\projetos\claude\apks\`. Commit `811b317`. **Falta só o campo:** dono instala build3, loga (token registra em `push_devices`), assume uma conversa e manda msg de lead → push em ≤2min. ⚠️ Backup dos 2 JSONs do Firebase em gerenciador de senhas; NUNCA recriar o projeto.

---
## 2026-08-11 (cont. IV) — 🚑 v7.116.0 conversas duplicadas (bug grave) + 🔔 v7.117.0 push do APK (aguarda Firebase)

Dono testou o APK à noite e reportou 3 problemas. **(1) IA re-pediu nome + saudação dupla → bug GRAVE de prod:** as msgs caíram em 3 conversas diferentes; causa-raiz = `.maybeSingle()` sem `.limit(1)` nos lookups do `whatsapp-webhook` (2+ abertas = PGRST116 engolido = conversa nova POR MENSAGEM; espiral chegou a **93 conversas duplicadas em 8 contatos**). Fix v7.116.0: limit(1)+erro logado, limpeza (85 resolvidas `dedup_fechada`, caso-relato reunificado), índice único parcial + retry-23505 (validados ao vivo). **(2) Foto da câmera travando:** telemetria pegou `upload→hang_timeout` (JPEG 3,2MB, `whatspro-app/1`); fix = ler File pra memória pré-upload (content:// pode pendurar no WebView) + telemetria de falha com `[norm:][upload:]`. **(3) Sem notificação = fase 3 esperada** → dono pediu HOJE: v7.117.0 shipada completa (push_devices RLS + push_alert_log + `push-queue-alert` cron 2min com anti-ruído da receita agro + OAuth WebCrypto + `appPush.ts` no AuthContext com logout-desliga + plugin no mobile, APK v1.1-build2, UA whatspro-app/2). Ciclo cron→fn validado (200 `firebase_not_configured` até o dono criar o projeto Firebase — checklist de 10min entregue). tsc 0 · vitest 2031/0 · deno 0 · types +60. Commits `d162b1b` + `008164e`. Lições em [[wiki/erros-e-licoes]] (4ª recidiva "supabase-js não lança").

---
## 2026-08-11 (cont. III) — 📸 v7.115.0: "Tirar foto" no menu de anexos + 🔑 rotação da senha admin

(1) **v7.115.0** — dono testou o APK no celular (funcionou: login, chat, menu) e pediu câmera direta. `ChatInput`: item "Tirar foto" (gate: UA `whatspro-app/` ou pointer coarse) → input `capture="environment"` → mesmo pipeline de envio; `cameraInputRef` novo no `useSendFile`. Sem novo APK (casca remota). tsc 0 · vitest 2031/0 · build ✓ · commit `909c029`; verificação live pelo marker "Tirar foto" no chunk `HelpDesk-*.js`. (2) **Senha do admin rotacionada** a pedido do dono (a antiga era fraca e a pendência estava aberta desde julho): hash bcrypt novo via SQL no `auth.users`, E2E dupla (senha nova loga ✓, antiga `invalid_credentials` ✓), `.env.local` atualizado — valor NUNCA no vault/memória, como manda a regra. Sessões antigas continuam ativas (avisado). (3) Registro: nome do lead Ivonete corrigido de manhã (entrada própria abaixo).

---
## 2026-08-11 (cont. II) — 👁️ v7.114.0: mostrar/ocultar senha no Login

Pedido direto do dono (print). `Login.tsx`: botão-olho no input de senha (alterna `password`/`text`), aria-label pt-BR, focável por teclado, `pr-11` pro texto não invadir o botão. tsc 0 · vitest 2031/0 · build ✓. Commit `4162a4c`, deploy via CI; verificação live pelo marker "Mostrar senha" no `index-*.js` (Login NÃO é lazy — diferente do Helpdesk).

---
## 2026-08-11 (cont.) — 📱 v7.113.0: APK Android do vendedor (Inbox+Fila) — casca Capacitor + build assinado no CI

Dono pediu APK do Inbox+Fila pros vendedores com câmera/galeria/microfone nativos, apontando o `casa-agricultor-v1.2-build3.apk` como referência. **Auditoria do APK** (é um ZIP) + descoberta do projeto-fonte em `C:\projetos\Claude\casa_do_agricultor\mobile\` — mesmo caso de uso, receita provada em campo (02-03/08). Dono aprovou via AskUserQuestion: **Opção A** (Capacitor `server.url` remoto → `crm.wsmart.com.br`), dashboard completo, push FCM na fase 3. **Shipado v7.113.0 (commit `dbb4d80`):** `mobile/` (Capacitor 7, appId `br.com.wsmart.whatspro`, UA `whatspro-app/1`), manifest com CAMERA/RECORD_AUDIO/MODIFY_AUDIO_SETTINGS/POST_NOTIFICATIONS + **bloco `<queries>` IMAGE_CAPTURE** (lição de campo do agro: sem ele a câmera não abre em parte dos aparelhos com targetSdk 35), assinatura PKCS12 condicional (keystore openssl alias `whatspro`, local gitignored, secrets `ANDROID_KEYSTORE_*` via `gh secret set`), ícones gerados (balão "W" no gradiente verde do painel), workflow `.github/workflows/apk.yml` (CI builda/assina; máquina local sem Java/SDK). **Zero código novo no front:** ChatInput + useAudioRecorder + downscale v7.111 já são o pipeline de mídia — o WebView só precisava das permissões. **Build 1 validado:** run 31514893059 success, "keystore escrito — assinatura de RELEASE", APK 3,8 MB conferido (config aponta pra prod, CERT.RSA presente) → salvo em `C:\projetos\claude\apks\whatspro-v1.0-build1.apk`. CI todo verde (quality-gate + docker + healthcheck + apk). **Fix colateral de raiz:** `check-md-length.sh` ignorava só `./node_modules` da raiz — `mobile/node_modules` estourou o pre-commit; agora `*/node_modules/*`. **Falta (pendências):** teste no celular real (instalar, logar, foto/galeria/áudio — só o dono pode), fase 3 push (Firebase+tokens+edge fn; armadilhas mapeadas no APP.md do agro), guardar keystore+senha em gerenciador de senhas. Detalhe: [[wiki/app-android]] + memória `project_android_app_capacitor_v7113`.

---
## 2026-08-11 — 🙏 Lead "Deus": greeting capturou vocativo religioso como nome (dado corrigido; fix de código proposto)

Dono printou: lead abriu com áudio transcrito *"Bom dia, Deus abençoe seu dia…"* e a IA respondeu **"Bom dia, Deus! Bem-vindo a Eletropiso"**. **Causa-raiz (confirmada no código):** no bloco de humanização do greeting (`ai-agent/index.ts:2170`, lead NOVO — roda SEM o gate `wasNameAsked`), o padrão #2 do `extractLeadName` (`nameCapture.ts:97` — "saudação + vírgula/espaço + palavra" pro caso "Boa tarde, João") casou com "Bom dia, **Deus** abençoe…" e capturou "Deus"; a palavra não está em `NON_NAME_WORDS` e o upsert do inline-name (`index.ts:2221`) **grava direto em `lead_profiles.full_name` sem passar pelo `sanitizeProfileName`** (que também deixaria passar). **Correção de dado em PROD:** `lead_profiles.full_name` 'Deus' → **'Ivonete'** (contact `ddaab2a3`, fone 558781104185); `contacts.name` já era "Ivonete" (pushname preservado, regra v7.78) e `lead_memory` não tinha nada a limpar. **Fix de raiz proposto (aguardando OK, HIGH RISK ai-agent):** adicionar vocabulário religioso/vocativo a `NON_NAME_WORDS` ('deus', 'abencoe/abençoe', 'amem/amém'…). Observação menor: perfil registrou interesses "arte religiosa/desenho" a partir da imagem de mãos em oração que a lead enviou — o interesse real é revestimento ripado branco (tags da conversa corretas).

---
## 2026-07-27 (cont.) — 🎧 v7.112.0: áudio do vendedor vira TEXTO + player/transcrição na Fila + foto do chat 50% menor

Dono testou o envio de imagem no localhost ("imagem enviada com sucesso!") e pediu 3 coisas. Usei `AskUserQuestion` em 2 pontos ambíguos e ele escolheu: imagem = **só exibição** (não recompressão) e **sim** transcrever áudio do vendedor. **Shipado v7.112.0 (commit `39297c5`):** (1) **Foto no chat 50% menor** — medi no browser antes de escolher o número (955px de largura, bolha 979px) → teto de **480px**; o `min(480px,100%)` inicial NÃO encolheu a bolha (percentual não entra na largura intrínseca: imagem 480 dentro de bolha 979 com faixa vazia), corrigido pra pixel puro `max-w-full sm:max-w-[480px]` → 480px em bolha de 504px; viewport 390px = 243px sem overflow. (2) **Modal da Fila** (`ConversationModal`) — `[audio]` seco virou `AudioPlayer` + 📝 transcrição (ou `(sem transcrição)` explícito) + rótulo cliente/enviado; mídia resolvida via `useMediaUrl` com `instance_id` da inbox (novo no SELECT) — **efeito colateral bom: as FOTOS do cliente voltaram a renderizar** (usavam `media_url` cru). +5 testes novos. (3) **Transcrição do áudio de SAÍDA** — auditoria antes de codar (agente Explore mapeou 10 leitores de `transcription`): incoming 219/221 transcritos, **outgoing 0/114** porque o webhook filtrava `direction==='incoming'`; o vendedor responde por áudio pelo CELULAR, então metade do atendimento era ilegível pro gestor e ficava fora de resumo/NPS/busca. Webhook liberado pra outgoing + **guard NA FONTE** no `transcribe-audio` (lê a `direction` da própria mensagem no DB; só dispara `ai-agent-debounce` se `incoming` — o disparo manda `direction:'incoming'` hardcoded, sem o guard a IA responderia ao próprio vendedor). **Validação em prod:** guard exercitado no áudio real do Thiago (Groq: *"O revestimento… é próprio só para a parede…"*) com `ai_agent_runs` 2→2 e nenhuma msg nova. **Backfill 7d: 26/37**; 11 deram **404 — URL de mídia do UAZAPI expira em ~4-5 dias** (22/07 morto, 23/07+ vivo). tsc 0 · deno 0 · vitest **2031/0** (+5) · build ✓. Deploy: `transcribe-audio` (guard) ANTES de `whatsapp-webhook` (ordem importa) + frontend via CI. **Pendência nova:** áudio é o ÚNICO tipo de mídia não espelhado no Supabase Storage → o histórico de áudio morre em dias (player não toca, transcrição não é mais recuperável); a transcrição agora ao menos salva o TEXTO. Recomendo espelhar no ingest, como já é feito com imagem.

---
## 2026-07-27 — 📷 v7.111.0: envio de imagem no Helpdesk consertado (4 fixes da auditoria de 26/07) — E2E real aprovado

Dono deu o OK ("corrija e teste") pros 4 fixes recomendados pela auditoria "vendedor Android não envia foto". **Shipado v7.111.0 (commit `2b0e54d`):** (1) **Downscale client-side** (`normalizeOutboundImage.ts`): JPEG/PNG >1MB → ≤2048px JPEG q0.85 pré-upload (contrato "nunca pior": falha/timeout de canvas em 20s ou re-encode que não reduz → envia o ORIGINAL; GIF/WEBP fora por animação; HEIC convertido também passa; PNG→JPEG com fundo branco). (2) **Timeout ≠ zumbi** (`uploadTimeoutPolicy.ts` puro + `useSendFile.ts`): no teto de 120s, `probeSession(4s)` decide — `valid` = falha limpa SEM reload (bolha de retry sobrevive), `unknown` = `recoverStuckSession()`, `dead` = `clearDeadSession()`. (3) **AVIF (`avif`/`avis`) + BMP nos magic bytes** → caminho de imagem via canvas (antes AVIF virava DOCUMENTO mudo com toast de sucesso). (4) **Telemetria de sucesso** (`stage=done/outcome=success` com duração+tamanho pós-normalização) + transporte `navigator.sendBeacon` (sobrevive ao reload); migration `media_telemetry_success_outcome` (CHECKs) + edge fn `log-send-failure` **v2** (whitelists). **E2E real via Playwright na PROD** (bundle novo verificado por markers nos chunks `HelpDesk-Cr2kA9L6`/`normalizeOutboundImage-B2_vDCCo`): login → conversa George Teste (sandbox Eletropiso `c4a5d342`) → upload de JPEG sintético **7,4MB 4000×3000** → enviado em **6,6s**, Storage recebeu **2048×1536 / 1,45MB (−80%)**, telemetria `success` gravada com `(downscaled)`, UAZAPI confirmou (foto entregue no WhatsApp do dono); estado restaurado (`ligada`, sem atribuição). tsc 0 · vitest **2026/0** (suíte 2012→2026) · build ✓ · deno 0. **Achados de sessão:** (a) `.git/index.lock` órfão de 26/07 21:42 tinha ABORTADO o commit de docs da v7.110.0 — vault ficou staged sem commit (release fantasma parcial); fechado em `1b88fd1` (cruza [[feedback_doc_without_commit_is_phantom_release]]); (b) **confirmado no YAML**: `deploy.yml` tem step "Redeploy via Portainer webhook" automático pós-build (secret `PORTAINER_WEBHOOK_URL`) — webhook manual NÃO é mais necessário, caveat removido do CLAUDE.md. **Monitorar:** taxa `success` vs falha por plataforma na `media_send_telemetry` (agora tem denominador); primeiro caso real de foto de câmera Android pós-fix.

---
## 2026-07-26 — 📚 v7.110.0: FAQ religada (R150) + falha transitória não sela shadow (R152) + 🔍 auditoria envio de imagem Android

Auditoria geral do projeto (protocolo de início + queries de saúde: **zero `router_fallback` real, cron do relatório 200 `sent:1` 2ª execução, 4 gestores ainda sem `personal_whatsapp`, 26 FAQs mortas confirmadas**) → dono aprovou atacar os itens 1 e 2. **Shipado v7.110.0 (commit `ad77ab9`, ai-agent v278):** (1) **R150** — load do knowledge restaurado no `index.ts` (cache 48h + sonda v7.103) e injetado nos 5 specialists via `specialistBase.knowledgeInstruction`; teste novo `specialistBase.test.ts` asserta o CONTEÚDO do prompt (o buraco da regressão). (2) **R152** — `llmErrorClassifier.ts` puro (408/429/5xx/timeout/breaker = transitório; 4xx/lógica = permanente), `llmCallLoop.errorMessage` cru, `routerPipeline.failure{transient,reason}`; index: transitório 1º strike = silêncio + tag `router_transient_fail:1` (próxima msg reprocessa, NÃO sela shadow), 2º strike consecutivo ou permanente = transbordo gracioso; strike limpa no turno OK. **E2E real:** FAQ-sentinela "7x sem juros" no Sandbox → injeção direta no webhook → router `pagamento` → objection specialist respondeu **"em até 7x sem juros"** (prompt 6.001 chars) → cenário limpo (FAQ deletada, conversa restaurada `desligada`). vitest **2012/0** (+17) · deno 0 · tsc 0.

**Auditoria "vendedor Android não envia imagem" (Helpdesk):** agente Explore mapeou o pipeline completo + verificação em prod. **Evidência:** telemetria `media_send_telemetry` tem SÓ 2 falhas em 45d — e a de **14/07 é Alberto, Android/Chrome, JPEG 4,8 MB, `upload → hang_timeout`** (a outra: Letícia, iPhone, 15/06, mesmo outcome). **Causa-raiz mais provável (cadeia):** foto de câmera sobe INTEGRAL (sem downscale client-side) → em rede móvel o teto de 120s do upload estoura → o timeout chama `recoverStuckSession()` que dá **`window.location.reload()`** (`sessionRecovery.ts:140`) → página recarrega, o `File` morre, a bolha de retry morre — vendedor vê "a tela piscou e não enviou". **Agravantes invisíveis:** (a) foto com `file.type` vazio + magic bytes `unknown` (AVIF/BMP/TIFF — AVIF não está em `HEIC_BRANDS`) vira **DOCUMENTO em silêncio** com `toast.success('Documento enviado!')`; (b) HEIC em Android: heic2any (asm.js) OOM em mobile e o fallback canvas SÓ decodifica HEIC no Safari → Android tem 1 tentativa; (c) telemetria cega — sem evento de sucesso, sem log do strike de picker, e o reload pode matar o beacon de falha. Bucket `helpdesk-media` é público (hipótese descartada) e imagens saem 20–222/dia (falha é por dispositivo/rede, não geral). **Fixes recomendados (aguardando OK):** downscale/compress client-side pré-upload (~2048px, q0.85 — mata a classe timeout, paridade WhatsApp); upload-timeout ≠ sessão-zumbi (só reload se sonda de sessão confirmar zumbi; senão falha limpa mantendo o retry vivo); AVIF nos magic bytes; telemetria de sucesso + `navigator.sendBeacon`.

---
## 2026-07-25 (cont. III) — 🏁 D6: monolito APOSENTADO (v7.109.0, ai-agent v277) — plano orquestrador 100%

Dono deu OK explícito ("prossiga") pro D6. Execução em 6 fases rastreadas: **(0) evidência** — 1.796 runs/30d 100% router+specialists, 0 monolith, 0 fallbacks; **(1) mapa cirúrgico** por agente Explore (monolith-only × compartilhado × router-only, mecânica exata do fallback return-or-fall-through); **(2) remoção** — index.ts **3.440→2.964 linhas (−476)**: prompt gigante, toolDefs, llmCallLoop do caminho antigo, sanitizer backstop, R130, Bug24v2, dispatchResponse do monolito, R129/R136/R121 inline, dispatchExitActionHandoff, contextDocuments (deletado), leadContext string, load do knowledge (−1 query/turno); routerPipeline sem gate/shadow; **fallback gracioso** no lugar do fallthrough (msg handoff + fila + shadow + nota + log router_fallback); UI sem seletor; migration default router; **(3) testes** — vitest 1995/0, deno ai-agent 0, tsc 0 (playground/debounce vermelhos = pré-existentes, provado via stash no HEAD); **(4) E2E real pós-deploy** — fluxo feliz EletropisoV2 (router produto 0.95 → qualification qualify-first; dedup segurou n8n atrasado) + **fallback forçado no Sandbox** (modelo inválido → transbordo digno + nota + fila + log; restaurado); **(5) prod limpa** (únicos eventos = do próprio E2E). **Achados:** FAQ/knowledge era monolith-only (specialists nunca receberam — religar é follow-up), n8n lag ~3min, Sandbox hospeda bot "Tamandaré" (E2E futuro = injeção direta no webhook; dois bots chegaram a conversar até eu desligar). Detalhe: memória `project_d6_monolith_retired_v7109`.

**Verificação em 2026-07-26** (dia SEGUINTE à sessão de 2026-07-25, ~40h pós-deploy): **ai-agent v277 ACTIVE**; tráfego com **router=40 turnos / qualification=16 / greeting=6 / product=3 / handoff=1**, os **14 turnos router-sem-specialist entregaram resposta ao lead** (curto-circuitos determinísticos) e **ZERO `router_fallback` em tráfego real** — o único registrado é o teste proposital de 2026-07-25 20:52 na conversa `4d52e0b7` do Sandbox. **CI verde nos 3 commits** (`quality-gate` com tsc+vitest duros).

**Verificação em 2026-07-26 — 1ª execução AUTOMÁTICA do cron do relatório** (v7.108.0): domingo **2026-07-26 18h30 SP** (jobid 50, runid 332575) → `net._http_response` 266287 status **200** `{ok:true, day:2026-07-26, sent:1}`, já no formato rico (deltas, categorias, pontos de atenção). O cron de **sábado (jobid 51) só será exercitado em 2026-08-01**.

---
## 2026-07-25 (cont. II) — 📊 Formato rico APROVADO como padrão diário + cron (v7.108.0)

Enviei ao dono um **resumo semanal rico** (deltas vs semana anterior, "o que procuraram" por categoria — Cerâmica 42 convs, Tinta 22 —, top 10 marcas, pico 9h/65% manhã, 5 pontos de atenção; achados novos: **só 4 msgs pelo painel na semana** e **1 enquete NPS/0 votos**) e ele aprovou: *"esse será o padrão relatório diário, enviado pro meu número"*. Shipado v7.108.0: **RPC v4** (`prev` mesmo-dia-semana-anterior + `category_mentions` via `p_categories` + `human_panel_msgs/convs` + `nps_sent`), **formatter rico** (deltas ▲/▼ com regra base<10 sem %, ↳ sub-linhas, `fmtMinutes`, ⚠️ 5 regras de atenção determinísticas; campos opcionais → layout legado preservado, `REPORT_CATEGORIES` fonte única), **edge fn** com `to_phone` fixo, **cron agendado** (dom-sex 18h30 SP + sáb 12h30 SP, padrão vault). E2E = disparo idêntico ao cron: 200 `sent:1`, relatório de hoje com "24 ▲20% (sáb ant.: 20)" no WhatsApp do dono. tsc 0 · deno 0 · vitest **2017/0** (+4) · types regen. Pendências: gestores `personal_whatsapp` (aí tirar `to_phone` do cron), config/UI toggle+horário, semanal-as-feature se o dono pedir. Detalhe: memória `project_daily_manager_report_v7106`.

---
## 2026-07-25 (cont.) — 🏷️ Resumo semanal one-off + auditoria "por que só 1 marca?" + fix de raiz (v7.107.0)

Dono pediu **resumo da semana 20–25/07** no WhatsApp → montei one-off (RPC diária 6× + agregação manual: 230 atendimentos, 956 msgs, 45 transbordos mediana 30min ⚠️ 9 sem resposta, 3 vendas, seg 20/07 mediana ~3h52) e enviei via UAZAPI. Ele questionou *"só 1 marca citada?"* e pediu top 10 → **auditoria profunda** (composição das msgs, scan estendido, freq. de tokens, amostras, tendência 4 semanas): **medição capturava 4% da realidade** (top_brands lia tags `set_tags` do LLM em `ai_agent_runs` — 1/semana — enquanto o detector R115 grava em `conversations.tags`, que a RPC nem lia), **11% das msgs vivem em `transcription`** (áudio/foto; foto = 1/3 das menções, describe-image lê embalagem), **dicionário sem cerâmicas** (core!), e causa de fundo COMPORTAMENTAL: ~10% das conversas citam marca, estável 4 semanas — cliente compra por categoria+cor+preço (*"qualquer marca com melhor preço"*), IA já pergunta. **Fix shipado (v7.107.0):** RPC v3 varre `content`+`transcription` com `p_brands` (fonte única `DEFAULT_BRANDS` TS, fallback legado se null), lista +25 marcas (sem Elizabeth/Karina — nome de pessoa), `brandDisplay` no formatter, limit 10. Smoke 6 dias (27 menções vs 1) + E2E real (relatório 23/07 ao dono, `sent:1`, "Brasilit (2) · Incenor (2)…"). tsc 0 · deno 0 · vitest **2013/0** (+3) · types regen. Deploy: migration + edge fn scoop. Backlog: brand-filter na qualificação. Detalhe: memória `project_daily_manager_report_v7106`.

---
## 2026-07-25 — 📊 Resumo do dia pros gestores por WhatsApp (v7.106.0) — E2E enviado ao dono

Dono mandou mockup de relatório diário (17h30, WhatsApp) — critiquei (números não fechavam: 34≠18+17, gráfico somava 58; corte 17h fixo perde o fim do dia; produtos sem contagem; `personal_whatsapp` dos gestores segue vazio) e ele aprovou a direção ("prossiga"). Implementado end-to-end na mesma sessão: **RPC `get_daily_manager_report`** (jsonb 1-chamada, janela SP, REVOKE anon/auth; migrations `daily_manager_report_rpc`+`_v2`) + **formatter puro `_shared/dailyReport.ts`** (15 testes; barras ▓ escaladas, faixa contínua de horas, "(fora do horário)" pelo dia da semana, seções omitidas quando vazias, NPS por bucket) + **edge fn `daily-manager-report`** (verifyCronOrService; `test_phone` OU gestores com `personal_whatsapp`; reusa `sendUazapiText`). **Contrato de consistência** espelhado RPC↔formatter (atendimento/novo/histograma/transbordo distintos por conversa — ver CHANGELOG). Sondar o RPC com os dados de ONTEM pegou 2 defeitos de definição pré-envio: transbordos contavam EVENTOS da fila (36→9 conversas) e a 1ª resposta humana ignorava vendedor pelo celular (`sender_id` NULL — reusei `human_handling_at`, lição v7.94). **E2E real:** 2 envios pro 5581993856099 via `net.http_post`+vault (caminho dos crons): hoje parcial + sexta completa (35 atend = 20+15 ✓, 161 msgs, 9 transbordos mediana 29min ⚠️ 1 sem resposta, 2 vendas) — `sent:1` cada, status 200. tsc 0 · deno 0 · vitest **2010/0**. Deploy: edge fn nova (CLI scoop, 8 assets bundlados). **Pendências:** dono validar formato no WhatsApp → decidir cadência (recomendo fechamento+30min por instância, não 17h30 fixo) → aí agendar cron + config/UI por agente; gestores cadastrarem `personal_whatsapp` pro envio real. Detalhe: memória `project_daily_manager_report_v7106`.

---
## 2026-07-09 — 🖼️ Foto de torneira → "água ou esgoto" (multi-categoria travada sob router) — v7.105.0

Dono printou: lead (Erika, EletropisoV2 PROD) mandou foto de torneira gourmet + "Qual o valor"; IA perguntou *"o cano é para água ou esgoto?"* e insistiu após "Não é cano não" → lead pediu atendente. **Auditoria via MCP DB (logs REAIS):** a VISÃO funcionou (`describe-image` descreveu "Torneira preta para cozinha com cano flexível…") e o router acertou ("torneira preta para cozinha", conf 0.9) — **não é falta de transcrição/contexto**. **Causa-raiz:** sob `routing_mode='router'` o R129 (desambiguação multi-categoria) é pulado (`index.ts:2597`), mas o auto-extract R143 continua e trava a 1ª categoria da ordem via `matchCategoryBySearchText`; "cano flexível" fez `canos` (ord 12) ganhar de `torneiras` (ord 13) → `interesse:canos` → field `tipo_cano` (examples "esgoto ou água"). **Fix (dono escolheu "confirmar com o lead"):** `preLLMAutoExtract.ts` detecta multi-categoria (`matchAllCategoriesBySearchText`≥2) e semeia `multi_interesse_pending` em vez de categoria única → qualification specialist pergunta "qual começar?" via `qualificationContext` (R129/R134, já roda sob router). Cirúrgico (1 arquivo + import), reusa infra existente; monolith intocado. +5 testes; deno 0; vitest **1995/0**. Deploy: ai-agent **v276** (CLI scoop). ⚠️ Nuance: EletropisoV2 nunca teve R129 → qualquer texto 2-categoria agora desambigua (ex.: "porta com fechadura") — monitorar falsos-positivos. Pendência: E2E (dono mandar foto de torneira). Detalhe: memória `project_torneira_cano_multicat_v7105`.

---
## 2026-07-06 — 🛡️ Auditoria de 2 erros de console da Fila → resiliência + a11y (v7.104.0, pendente deploy)

Dono printou 2 erros no console de `/dashboard/fila`. Auditei com workflow (3 investigadores + verificação adversarial) e o verificador derrubou 1 exagero (o `CommandDialog` NÃO emitia o warning de Description — o hack o suprimia) e achou 1 acoplamento (remover o hack reintroduz o warning em ~21 dialogs). **Erro 1** (`ERR_NAME_NOT_RESOLVED`) = DNS transitório do cliente (recarregou OK), não código — mas expôs o **empty-state enganoso**: a aba "Sem atend." mostrava "Nenhum lead esperando 🎉" quando a query FALHAVA (`data=undefined→[]`, nenhum hook lia `isError`), enganando o gestor. **Erro 2** (`requires a DialogTitle`) = `CommandDialog` sem título, montado em toda rota via `GlobalSearchDialog`. **6 fixes (todos frontend):** (1+4) `QueueDashboard`/`UnattendedLeadsTab`/`LiveHeader` leem `isError`+`refetch`, card de erro + retry, badge "!" em erro; (3) `QueryCache({onError})` global + retry 1→2 em `App.tsx`; (2) `DialogTitle`/`DialogDescription` sr-only no `CommandDialog`+`SendStatusModal`; (6) `NotificationBell` consome `error`; (5) removido o hack `aria-describedby:undefined` de `dialog.tsx` (confirmado no dist do Radix 1.1.14 que quebrava a11y dos 26 dialogs com Description) + workflow idempotente (8 agentes) cobriu os ~21 dialogs sem descrição → **54:54 DialogContent↔DialogDescription**. tsc 0 · vite build ✓ · vitest **1990/0**. **Shipado + LIVE** (commits `a6f5460`+`bbe6a30` → CI → webhook Portainer; bundle live `index-MiQmLCbH.js` com markers do QueryCache novo — verificado, não é release-fantasma). **O quality-gate quebrou num flaky pré-existente** (`dateUtils.test.ts` `smartDateBR "Ontem"`: teste montava "ontem" no fuso do runner, fn avalia em SP, CI rodou 01:38 UTC) → corrigido de raiz com `vi.setSystemTime` (commit `bbe6a30`), validado sob `TZ=UTC`. Detalhe: memória `project_session_2026_07_06_dialog_a11y_queue_resilience`.

---
## 2026-07-02 (cont. III) — 📄 PRD-modelo do Disparador (docs/prd-modelo-disparador.md)

Dono pediu PRD do Disparador incluindo carrossel. Agente varreu o módulo (50+ arquivos) e escrevi o **PRD reutilizável grounded na implementação real** (194 lin, mesmo formato do prd-modelo-bio-link): grupos+leads, 4 tipos de msg, importação 4 fontes + verificação, bases (merge/move/auto-enroll+reconcile), agendamento recorrente (claim SKIP LOCKED), anti-ban (delays 350ms/5-10s/10-20s), relatórios, §4 dedicada ao **carrossel** (2-10 cards, 3 botões URL/REPLY/CALL, pipeline de imagem: nunca base64 → URL pública, upload memoizado 1x, cacheControl 30d) + armadilhas de produção e métricas.

---
## 2026-07-02 (cont. II) — 🧹 v7.103.0 cache 48h c/ sonda + faxineiro 48h

Dono: "a cada 48h limpar o cache e o que está impactando pra voltar ao Free". Fui crítico com a premissa (cache de 60s auto-limpa; limpar não ajuda) e entreguei o que maximiza o objetivo: **cache vale 48h + sonda `updated_at` de ~100 bytes por turno** (config/FAQ propagam no turno seguinte; 20 kB só trafegam quando algo mudou). Migration `20260702190000`: trigger `ai_agent_knowledge_updated_at` (faltava — edição de FAQ não bumpava a sonda) + backfill + **`purge_stale_operational_data()` em cron 48h** (jobs: logs>30d, runs/eventos/notifs>60d; 0 FKs; dashboards olham ≤30d) — **1ª execução real purgou 2.301 logs**; + `cleanup-old-media` v4 cobre `carousel-images`. deno/tsc 0 · vitest 1990/0. Deploy: ai-agent **v275**, cleanup-old-media **v4**. Commit `1e26d31`. **Régua de volta ao Free:** egress/dia no painel <~115 MB por 1 ciclo (5 GB c/ 30% folga) + storage <1 GB (retenções ativas) + DB <500 MB (faxineiros) → downgrade com crédito pro-rata.

---
## 2026-07-02 (cont.) — 📉 Dieta de egress executada (v7.102.0) + types.ts regen destrava healthcheck

"Prossiga com a dieta" → medi ANTES de cortar (regra da casa): agent row = 20 kB, KB = 12 kB total, lista helpdesk 50 conv = 77 kB, RPCs de fila role-gated (403 como postgres — correto); janela viva no gateway mostrou que o tráfego constante é o pipeline (crons+agente), não browser. **3 fixes shipados (commit `c8d11b9`):** (1) **cache por-isolate TTL 60s** pro agent row + knowledge no ai-agent (`agentConfigCache.ts` puro, +6 testes; verificado 0 mutações de `agent.x` no pipeline antes de cachear); (2) **gate de visibilidade + coalescing** nos refetches por broadcast do painel de Fila (`broadcastRefetch.ts`, +5 testes) — aba em background não refaz RPC (o canal realtime não pausa sozinho; só o polling pausa), rajada vira 1 refetch/5-10s; (3) **carrossel do Disparador sobe 1x** — memoização por `File` no `uploadCarouselImage` (era re-upload por destinatário + espelho, objeto novo por jid) + cacheControl 1h→30d (CDN passa a amortizar os N downloads da UAZAPI em cached egress, cota separada). **Achado no push:** o workflow **Vault & Schema Healthcheck falhava em TODO push desde 25/06** — `types.ts` sem regen pós-migration do NPS (`nps_scale`/`numeric_score` no `usePollMetrics`); regen via scoop (+55 lin aditivas; ⚠️ `>` do PS 5.1 grava UTF-16 — usar bash) → check verde (commit `3df7a65`). tsc 0 · deno 0 · vitest **1990/0** (+11). Deploy: ai-agent **v274** + frontend CI. Estimativa de corte: dashboards em background (maior fatia variável) + ~0,5 GB/mês do agente + campanhas futuras sem multiplicador.

---
## 2026-07-02 — 🔴→🟢 OUTAGE: Free estourou egress (8,42/5 GB) → upgrade PRO → auditoria dashboard → fix escalate (v7.101.1)

**Outage (dono: "não consigo logar"):** projeto restrito por `exceed_egress_quota` → **402 em TUDO** (auth/REST/realtime/edge fns) de 01/07 22:15 UTC a 02/07 13:57 UTC (~15h40; webhook rejeitado = **msgs de leads perdidas** — equipe varrer aparelhos). Workflow (4 agentes) + Management API: plano FREE, ciclo reseta dia 5 (org criada 05/05), sem spend cap pra remover. **Por que a regra "Free Forever" falhou:** a auditoria 06-10 automatizou pausa/DB/storage/MAU/invocações, mas egress ficou "acompanhar no painel" (única dimensão MANUAL — API exige login, PAT dá 401) e nunca virou rotina; aviso do Supabase foi pro email de billing despercebido. Dono fez **upgrade Pro** ($25, spend cap ON, fatura paga, ciclo reancorado 02/07→02/08); restauração verificada E2E (login real 200 + pipeline 200s). **Auditoria com dashboard logado (Playwright; hCaptcha derrotou a automação — 4 tentativas, respostas certas na tela 1, `challenge-expired`/400 nas finais — dono clicou):** egress do ciclo = **8,42 GB, cached só 0,35 GB**; breakdown por serviço: dia normal **PostgREST 72,9%** + Storage 25,8%; pico 15/06 (668 MB) Storage 67,2% → dieta mira 1º payload da Data API (ai-agent recarrega config+KB/turno; polling QueueDashboard 10/15/30s + refetch por broadcast), 2º thumbnails/cache de mídia. Billing/saúde/advisors OK (99 achados = backlog conhecido).

**Fix v7.101.1 (achado da auditoria, "60% errors" no DATABASE):** `escalate-stale-handoffs` (cron 2min) selecionava **`conversations.contact_name`/`contact_phone` — colunas que NUNCA existiram** (desde 07/05): 42703 em rajada a cada run, re-ping "o lead cliente", `alertManagers` early-return → **alerta de lead órfão ao gestor nunca disparou**; e rows nunca assentadas re-tentavam pra sempre (**1.323 + 1.255 presas**). Fix: embed `contact:contacts(name, lead_profiles(full_name))` + `pickLeadName` + `settle()` em todo caminho de decisão (incl. lock v7.94 e vendor-respondeu); **backfill assentou o estoque ANTES do deploy** (zero alerta retroativo). deno 0; prod: run 3,7-4,4s→**~0,5s**, 0 erro novo. Commit `9d543bd`. **Bônus:** HIBP "leaked passwords" LIGADO no Auth (pendência 06-10). **Pendências:** rotacionar senha dashboard (trafegou no chat) + PAT; sprint dieta de egress (meta <3,5 GB/mês se quiser voltar ao Free — downgrade é imediato c/ crédito pro-rata); achado menor `chk_ai_agent_logs_event` violado (~4/h, ai-agent HIGH RISK → backlog). Detalhe: memória `project_egress_quota_outage_2026_07_02`.

---
## 2026-06-28 — 📊 Dashboard do Gestor: destaque no menu + "Últimas 24h" + Eletropiso default (v7.99.0) + 🗝️ mapa de credenciais

**Dashboard (v7.99.0):** 3 pedidos do dono (prints): Gestão como item 1 em destaque no menu; instância Eletropiso 558781592373 default; opção "Últimas 24h" no filtro. 1ª versão entregue rápido; **auditoria por workflow (23 agentes, 17 achados → 13 confirmados, 11 in-scope)** revelou fix parcial — `PERIOD_OPTIONS`/texto "últimos N dias" são compartilhados por 5 telas / 7 lugares (cobri 2). **Refiz de raiz:** helpers únicos `formatPeriodLabel` (7 consumidores) + `resolveDefaultManagerInstance` (5 telas — mata o flip-flop do widget Assistente); `Sidebar` ganhou param `highlight` e o grupo Gestão foi pro topo; clamp `conversionRate` em 100% (numerador dia-granular furava sob 24h); `dot` condicional nos 2 gráficos (1 ponto não some). Frontend puro, nenhuma edge fn. tsc 0 novo · vitest **1979/0** · lint 0 novo. 18 editados + 2 libs novas (`src/lib/periodLabel.ts`, `src/lib/managerInstanceDefault.ts`). Deploy: push master → CI → webhook Portainer.

**Credenciais (docs):** auditei vault + memória + código e consolidei o acesso num **mapa único**: novo `wiki/acesso-credenciais.md` (Supabase, env vars do frontend, secrets das edge fns por NOME, n8n, UAZAPI, Portainer, ponteiros pros segredos), seção "Deploy, Supabase & Acesso" expandida no `CLAUDE.md` (+ MCP `execute_sql` 403, ingestão n8n, UAZAPI), memória `reference_access_credentials_map` + entrada no índice. **Nenhum valor de segredo commitado** (só nomes/ponteiros). Drift corrigido: o código usa `INTERNAL_FUNCTION_KEY`, não `CRON_AUTH_KEY`. Sinalizadas ao dono pendências de segurança: rotacionar PAT (vazou em chat 2026-05-06) + senha admin fraca.

**SLA card modal + MCP + validação (v7.99.1):** descobri que o MCP **`mcp__supabase-novo` é o NOSSO** (`mcp__supabase` sem sufixo = outra conta, 403) → tenho SQL no projeto. **Validei os números do dashboard em prod** (instância V2 `re662a6d32de7e0`): card "Sem resposta +30min" = **23** (exato), lista idêntica (Erick/Luis/Lucas Gois/Lucas Rodrigues/SLONE), Score 40 (exato), Conversão 107/551 = 19% (exato), Leads 551 (print 568 = janela de 30d deslizou). **Achado:** `dash_sla_sem_resposta` não tem teto de recência → lista leads de 38-39 dias (backlog de abandonados, não SLA-agora; decisão de produto do dono). **Feature:** o card SLA (`SlaAlertList`) abria a conversa em nova aba → agora abre **em modal** (reusa `ConversationModal` + a11y role/Enter). tsc 0 · vitest 1979/0. Deploy: v7.99.1.

**Cards "Atendimento" (v7.100.0):** pedido do dono nos 3 cards (`PendingConversationsCard`: sem 1ª resposta / sem resposta +24h / cotações) — mostrar a **msg do lead** + **atendente atribuído**, clicar abrir **modal**, e paginação p/ não travar o load (282 pendentes num card). Implementado: RPC nova aditiva `get_pending_conversation_previews(ids[])` (INVOKER, RLS; migration `20260628170000`, aplicada em prod via `mcp__supabase-novo` + testada) buscada **só da página visível** (lazy); hook `usePendingConversationPreviews` (`keepPreviousData`); card mostra msg+atendente (placeholder neutro enquanto carrega, "Não atribuído" em vermelho só quando confirmado), clique→`ConversationModal`, "Ver mais" (8/vez). Auditado por workflow (11 agentes, 0 blocker/high, 2 LOW fechados). tsc 0 novo · vitest 1979/0 · build OK. **CHANGELOG particionado** (298→224): cluster 06-11 v7.84.0→v7.80.0 → `wiki/changelog/2026-06-part5.md`. Deploy: v7.100.0.

**Teto de 7 dias nos cards (v7.101.0):** dono pediu "só os últimos 7 dias" (os cards listavam backlog de 38-39 dias). `get_abandoned_conversations`/`dash_sla_sem_resposta`/`get_active_quotes` ganharam `>= now() - interval '7 days'` (CREATE OR REPLACE mesma assinatura; migration `20260628180000`, aplicada via `mcp__supabase-novo`); unanswered via `p_days_lookback=7` no hook. Prod: +24h **282→32**, SLA **23→7**, cotações **5→0**, 1ª-resposta **4→0** (zerados = backlog 100% >7d). tsc 0 · vitest 1979/0. Deploy: v7.101.0.

---
## 2026-06-26 — 📇 Contato compartilhado (vCard) → saudação + transbordo (v7.98.0)

Dono mandou print: lead compartilhou um CONTATO no WhatsApp ("Fernando Amaral Caprice") e a IA respondeu *"Esse não é o nosso forte aqui, mas trabalhamos com outros materiais relacionados…"* — pediu que ao receber um contato a IA **não responda/venda**, só saúde + transborde pra um atendente. **Investigação:** rastreei a ingestão — `whatsapp-webhook` (`normalizeMediaType` → `'contact'`) salva a mensagem de contato com o **displayName como `content`** (`index.ts:766`); o `ai-agent` lê isso como `incomingText` (via `loadIncomingMessages`, que carrega `media_type` por msg) e o LLM trata o nome como pergunta de produto. O agente **não tinha tratamento de vCard**. **Decisões do dono (AskUserQuestion):** ativação = **GLOBAL** (todos os agentes, sem config — vCard nunca é consulta de produto); contato+texto junto = **sempre transbordar**. **Fix (de raiz, espelhando jobVacancy + sale_closed):** módulo puro novo `_shared/agent/contactShareHandoff.ts` (`detectSharedContact` / `greetingForHour` / `buildContactShareReply`, +11 testes) + wire no `index.ts` logo após `runQueueAssignment` (~linha 1176, ANTES de greeting/produto/excluídos): se `detectSharedContact(incomingMessages)` e não-shadow e não-`shadow_only` → envia saudação por horário (fuso `America/Sao_Paulo`) + agradecimento + aviso de transbordo, depois `runQueueAssignment` (fila/atribuição + `notify-vendor-assignment` fire-and-forget de dentro do `assignHandoff`) + `status_ia=SHADOW` + tags de handoff + nota interna "📇 …" + log `implicit_handoff reason=contact_shared` + Response. **Auditoria:** confirmei que `seller_notified:'true'` NÃO suprime a notif ao vendedor (ela vem do `assignHandoff`, idempotente por `notification_log`); idempotência do handoff garantida pelo SHADOW + guard próprio. tsc 0 · deno check ai-agent 0 · vitest **1976/0** (+11). Deploy: ai-agent **v272** (scoop `--use-api`, bundla o módulo novo) — global, todos os agentes.

**(cont.) v7.98.1 — E2E do dono REPROVOU a v7.98.0; fix na detecção.** Dono enviou "Boa tarde" (greeting OK) + um contato ("Lara Eletropiso Lucas") → IA respondeu *"O que você está procurando?"* (não transbordou). **Ground truth no DB:** o contato NÃO chega como `media_type='contact'` — chega `media_type='text'` com `content="Lara Eletropiso Lucas\nPhone: +55 87 99676-2520"`. O pipeline UAZAPI → **n8n** → webhook **achata o vCard em texto** (webhook lê `message.mediaType||messageType||type`, todos 'text'); meu `detectSharedContact` por `media_type` nunca casava. **Fix:** detecta também pelo PADRÃO do texto — linha `Phone: +<número>` (ou `BEGIN:VCARD`) que só o app gera (lead não digita "Phone:" em inglês); mantém o check por `media_type` como defesa. Testes usam a STRING real capturada + caso falso-positivo ("meu telefone é 87…" não dispara). +3 testes → vitest **1979/0**, tsc 0, deno 0. Deploy: ai-agent **v273**. Lição nova (n8n transforma a msg — confirmar formato real no DB antes de detectar por tipo). **⚠️ Pendência:** dono re-testar enviando um contato. Detalhe: memória `project_contact_share_handoff_v798`.

---

## Entries arquivadas

Entries de 2026-06-04 a 2026-06-25 (v7.73.0–v7.97.x) → [[wiki/log-arquivo-2026-06-04-a-06-25]].
Entries de 2026-06-02 a 2026-06-03 (v7.66.0–v7.72.0) → [[wiki/log-arquivo-2026-06-02-a-06-03]].
Entries de 2026-05-29 a 2026-06-01 (v7.57.4–v7.65.1) → [[wiki/log-arquivo-2026-05-29-a-06-01]].
Entries de 2026-05-28 (v7.57.0–v7.57.3) → [[wiki/log-arquivo-2026-05-28-part2]].
Entries de 2026-05-26 e anteriores → [[wiki/log-arquivo-2026-05-28-part1]].
Para arquivos mais antigos, ver `wiki/log-arquivo-*`.
