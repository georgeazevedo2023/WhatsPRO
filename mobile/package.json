{
  "name": "whatspro-app",
  "private": true,
  "version": "1.0.0",
  "description": "APK do vendedor/atendente (Inbox + Fila) — WebView do painel em produção (crm.wsmart.com.br). Receita e decisões: wiki/app-android.md",
  "scripts": {
    "sync": "cap sync android",
    "icons": "capacitor-assets generate --android"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/core": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5",
    "@capacitor/cli": "^7.0.0"
  }
}
