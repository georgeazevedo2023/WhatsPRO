---
phase: 07-consolidacao-de-helpers-e-reducao-de-duplicacao-backend
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/functions/_shared/supabaseClient.ts
  - supabase/functions/_shared/supabaseClient.test.ts
  - supabase/functions/_shared/carousel.ts
  - supabase/functions/_shared/carousel.test.ts
  - supabase/functions/_shared/auth.ts
  - supabase/functions/_shared/llmProvider.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "createServiceClient() returns a Supabase client using SERVICE_ROLE_KEY"
    - "createUserClient(req) returns a Supabase client scoped to the user's JWT"
    - "auth.ts uses supabaseClient.ts instead of inline createClient"
    - "generateCarouselCopies and buildCarousel live in _shared/carousel.ts"
    - "LLMResponse includes latency_ms field"
  artifacts:
    - path: "supabase/functions/_shared/supabaseClient.ts"
      provides: "Centralized Supabase client factory"
      exports: ["createServiceClient", "createUserClient"]
    - path: "supabase/functions/_shared/carousel.ts"
      provides: "Carousel building + AI copy generation + LRU cache"
      exports: ["buildCarousel", "generateCarouselCopies", "cleanProductTitle", "CAROUSEL_CACHE_TTL_MS", "CAROUSEL_CACHE_MAX_SIZE"]
    - path: "supabase/functions/_shared/supabaseClient.test.ts"
      provides: "Unit tests for client factories"
    - path: "supabase/functions/_shared/carousel.test.ts"
      provides: "Unit tests for carousel logic + cache"
  key_links:
    - from: "supabase/functions/_shared/auth.ts"
      to: "supabase/functions/_shared/supabaseClient.ts"
      via: "import { createServiceClient, createUserClient }"
      pattern: "import.*from.*supabaseClient"
    - from: "supabase/functions/_shared/llmProvider.ts"
      to: "LLMResponse.latency_ms"
      via: "Date.now() timing in callOpenAI/callGemini"
      pattern: "latency_ms"
---

<objective>
Create the foundational shared modules that all subsequent migration plans depend on: supabaseClient.ts (D-01), carousel.ts (D-03), LLM metrics (D-05), and update auth.ts to use the new client factory (D-01 prerequisite — auth.ts has inline createClient calls that must be eliminated first since other functions import from auth.ts).

Purpose: Establish the import targets before migrating 28 functions in subsequent plans.
Output: 4 new files + 2 updated files in _shared/, all with unit tests.
</objective>

<execution_context>
@C:/Users/georg/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/georg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-consolidacao-de-helpers-e-reducao-de-duplicacao-backend/07-CONTEXT.md
@.planning/phases/07-consolidacao-de-helpers-e-reducao-de-duplicacao-backend/07-RESEARCH.md

<interfaces>
<!-- Existing shared utilities the new files must integrate with -->

From supabase/functions/_shared/response.ts:
```typescript
export function successResponse<T>(corsHeaders: CorsHeaders, data: T, status?: number): Response
export function errorResponse(corsHeaders: CorsHeaders, error: string, status?: number, details?: string): Response
```

From supabase/functions/_shared/logger.ts:
```typescript
export function createLogger(functionName: string, requestId?: string): Logger
// Logger has: debug, info, warn, error methods
```

From supabase/functions/_shared/auth.ts (WILL BE MODIFIED):
```typescript
// Currently has 2 inline createClient() calls at lines 11 and 32
// Must migrate to import from supabaseClient.ts
export async function verifyAuth(req: Request): Promise<{ userId: string } | null>
export async function verifySuperAdmin(req: Request): Promise<{ userId: string } | null>
export function verifyCronOrService(req: Request): boolean
export function unauthorizedResponse(corsHeaders: Record<string, string>): Response
```

From supabase/functions/_shared/llmProvider.ts:
```typescript
export interface LLMResponse {
  text: string
  toolCalls: { name: string; args: Record<string, unknown>; id: string }[]
  inputTokens: number
  outputTokens: number
  model: string
  provider: 'openai' | 'gemini'
  // latency_ms will be ADDED per D-05
}
export async function callLLM(req: LLMRequest): Promise<LLMResponse>
```

From supabase/functions/_shared/fetchWithTimeout.ts:
```typescript
export function fetchWithTimeout(url: string, init: RequestInit, timeoutMs: number): Promise<Response>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create supabaseClient.ts + carousel.ts + unit tests</name>
  <files>
    supabase/functions/_shared/supabaseClient.ts
    supabase/functions/_shared/supabaseClient.test.ts
    supabase/functions/_shared/carousel.ts
    supabase/functions/_shared/carousel.test.ts
  </files>
  <read_first>
    supabase/functions/_shared/auth.ts
    supabase/functions/_shared/circuitBreaker.test.ts
    supabase/functions/ai-agent/index.ts (lines 1-148 for carousel logic + env vars)
    supabase/functions/_shared/fetchWithTimeout.ts
  </read_first>
  <action>
    **supabaseClient.ts** (per D-01):
    Create file with 2 exported functions:
    - `createServiceClient()`: reads `Deno.env.get('SUPABASE_URL')!` and `Deno.env.get('SUPABASE_SERVICE_ROLE_KEY')!`, returns `createClient(url, key)`. Import `createClient` from `https://esm.sh/@supabase/supabase-js@2`.
    - `createUserClient(req: Request)`: extracts `Authorization` header from req, reads `SUPABASE_URL` and `SUPABASE_ANON_KEY` from env, returns `createClient(url, anonKey, { global: { headers: { Authorization: authHeader } } })`.

    **supabaseClient.test.ts**: Follow pattern from `circuitBreaker.test.ts`. Mock `Deno.env.get` using `globalThis.Deno = { env: { get: (k) => envMap[k] } }` pattern. Test:
    - createServiceClient() returns truthy client object
    - createUserClient(req) passes Authorization header through
    - Missing env vars throw (the `!` assertion)

    **carousel.ts** (per D-03):
    Extract from `ai-agent/index.ts` lines 19-148:
    - `cleanProductTitle(title: string): string` (lines 20-36)
    - `COPY_PROMPT` const (lines 39-47)
    - `parseCopyResponse(text: string, count: number): string[] | null` (lines 49-57)
    - `_carouselCopyCache` Map + `CAROUSEL_CACHE_TTL_MS` + `CAROUSEL_CACHE_MAX_SIZE` (lines 62-64)
    - `generateCarouselCopies(product: any, numCards: number): Promise<string[]>` (lines 66-148)
    - Import `fetchWithTimeout` from `./fetchWithTimeout.ts`
    - Read `GROQ_API_KEY`, `GEMINI_API_KEY`, `MISTRAL_API_KEY` inside function body via `Deno.env.get()` (not module-level — avoids closure issue per RESEARCH pitfall 3)
    - Export: `cleanProductTitle`, `generateCarouselCopies`, `CAROUSEL_CACHE_TTL_MS`, `CAROUSEL_CACHE_MAX_SIZE`, `parseCopyResponse`

    **carousel.test.ts**: Test:
    - `cleanProductTitle` removes redundant brand segments (test "Tinta Coral - Coral Premium - Coral" -> strips last)
    - `parseCopyResponse` extracts array from JSON in text
    - `parseCopyResponse` returns null on invalid input
    - Cache eviction at MAX_SIZE (mock the Map, insert MAX_SIZE+1 entries)
  </action>
  <verify>
    <automated>cd C:/Projetos/Claude/WhatsPRO && npx vitest run supabase/functions/_shared/supabaseClient.test.ts supabase/functions/_shared/carousel.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - supabaseClient.ts exports createServiceClient and createUserClient
    - carousel.ts exports cleanProductTitle, generateCarouselCopies, parseCopyResponse, CAROUSEL_CACHE_TTL_MS, CAROUSEL_CACHE_MAX_SIZE
    - All unit tests pass
    - grep -c "createServiceClient\|createUserClient" supabase/functions/_shared/supabaseClient.ts returns >= 2
    - grep -c "generateCarouselCopies\|cleanProductTitle" supabase/functions/_shared/carousel.ts returns >= 2
  </acceptance_criteria>
  <done>supabaseClient.ts and carousel.ts exist with correct exports. Unit tests pass for both. No existing tests broken.</done>
</task>

<task type="auto">
  <name>Task 2: Update auth.ts to use supabaseClient.ts + add LLM metrics to llmProvider.ts</name>
  <files>
    supabase/functions/_shared/auth.ts
    supabase/functions/_shared/llmProvider.ts
  </files>
  <read_first>
    supabase/functions/_shared/auth.ts
    supabase/functions/_shared/llmProvider.ts
    supabase/functions/_shared/supabaseClient.ts (just created in Task 1)
  </read_first>
  <action>
    **auth.ts** (per D-01 — prerequisite for all function migrations):
    - Remove line 1: `import { createClient } from 'https://esm.sh/@supabase/supabase-js@2'`
    - Add: `import { createServiceClient, createUserClient } from './supabaseClient.ts'`
    - In `verifyAuth()` (line 11): replace `const supabase = createClient(Deno.env.get('SUPABASE_URL')!, Deno.env.get('SUPABASE_ANON_KEY')!, { global: { headers: { Authorization: authHeader } } })` with `const supabase = createUserClient(req)`. NOTE: verifyAuth currently manually extracts the auth header and passes it — createUserClient does the same thing internally. The `token` variable (line 17) still needs to be extracted separately for `supabase.auth.getUser(token)` call — keep `const token = authHeader.replace('Bearer ', '')`.
    - In `verifySuperAdmin()` (line 32): replace `const serviceClient = createClient(Deno.env.get('SUPABASE_URL')!, Deno.env.get('SUPABASE_SERVICE_ROLE_KEY')!)` with `const serviceClient = createServiceClient()`
    - In `verifyCronOrService()`: replace the 2 `console.error` and 1 `console.log` calls with structured logger. Add `import { createLogger } from './logger.ts'` and create logger at function call: `const log = createLogger('auth')`. Replace: `console.error('[auth] Invalid auth...')` -> `log.error('Invalid auth header format or missing')`, `console.error('[auth] Token mismatch...')` -> `log.error('Token mismatch', { tokenLength: token.length, hasService: !!serviceKey, hasAnon: !!anonKey, hasInternal: !!internalKey })`, `console.log('[auth] verifyCronOrService...')` -> `log.info('verifyCronOrService successful', { mode })`
    - Do NOT change the function signatures or return types

    **llmProvider.ts** (per D-05):
    - Add `latency_ms: number` to the `LLMResponse` interface
    - In `callOpenAI()`: add `const startMs = Date.now()` as first line. Add `latency_ms: Date.now() - startMs` to the return object.
    - In `callGemini()`: same pattern — `const startMs = Date.now()` at top, `latency_ms: Date.now() - startMs` in return.
    - In `callLLM()`: after each successful `callOpenAI`/`callGemini` return, the result already includes `latency_ms` — no change needed in callLLM itself.
    - Replace the 3 `console.warn` calls in `callLLM()` with structured logging: add `import { createLogger } from './logger.ts'` and `const log = createLogger('llm-provider')` at module level. Replace:
      - `console.warn('[llm] Gemini failed...')` -> `log.warn('Gemini failed, falling back to OpenAI', { error: (err as Error).message })`
      - `console.warn('[llm] OpenAI failed...')` -> `log.warn('OpenAI failed, falling back to Gemini', { error: (err as Error).message, breakerOpen: openaiBreaker.isOpen })`
      - `console.warn('[llm] OpenAI circuit breaker OPEN...')` -> `log.warn('OpenAI circuit breaker OPEN, skipping to Gemini')`
  </action>
  <verify>
    <automated>cd C:/Projetos/Claude/WhatsPRO && npx vitest run --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - grep "import.*supabaseClient" supabase/functions/_shared/auth.ts returns match
    - grep -c "createClient.*esm.sh" supabase/functions/_shared/auth.ts returns 0 (no more inline imports)
    - grep "latency_ms" supabase/functions/_shared/llmProvider.ts returns matches in LLMResponse + callOpenAI + callGemini
    - grep "createLogger" supabase/functions/_shared/llmProvider.ts returns match
    - grep "createLogger" supabase/functions/_shared/auth.ts returns match
    - All existing tests pass (npm run test)
  </acceptance_criteria>
  <done>auth.ts uses supabaseClient.ts with zero inline createClient. llmProvider.ts includes latency_ms in LLMResponse and uses structured logging. All tests pass.</done>
</task>

</tasks>

<verification>
- `supabaseClient.ts` exports 2 functions, no inline `createClient` in `auth.ts`
- `carousel.ts` contains all carousel logic extracted from ai-agent
- `llmProvider.ts` LLMResponse has `latency_ms` field
- All unit tests pass: `npx vitest run --reporter=verbose`
- No existing tests broken
</verification>

<success_criteria>
- 4 new files created (_shared/supabaseClient.ts, supabaseClient.test.ts, carousel.ts, carousel.test.ts)
- 2 files updated (auth.ts, llmProvider.ts)
- Zero inline `createClient` calls remaining in auth.ts
- latency_ms present in LLMResponse interface and both provider functions
- All tests green
</success_criteria>

<output>
After completion, create `.planning/phases/07-consolidacao-de-helpers-e-reducao-de-duplicacao-backend/07-01-SUMMARY.md`
</output>
