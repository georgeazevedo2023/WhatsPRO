---
phase: 07-consolidacao-de-helpers-e-reducao-de-duplicacao-backend
plan: 04
type: execute
wave: 3
depends_on: ["07-01", "07-02", "07-03"]
files_modified:
  - supabase/functions/transcribe-audio/index.ts
  - supabase/functions/whatsapp-webhook/index.ts
  - supabase/functions/uazapi-proxy/index.ts
  - supabase/functions/ai-agent/index.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "4 large functions use createServiceClient/createUserClient instead of inline createClient"
    - "4 large functions use successResponse/errorResponse instead of new Response(JSON.stringify(...))"
    - "4 large functions use createLogger instead of console.log/error"
    - "ai-agent imports generateCarouselCopies and cleanProductTitle from _shared/carousel.ts"
    - "All 4 'Confira:' occurrences in ai-agent replaced with agent.carousel_text || 'Confira:'"
    - "Zero behavior change across all 4 functions"
    - "Zero inline createClient calls remain across ALL 28 edge functions"
  artifacts:
    - path: "supabase/functions/ai-agent/index.ts"
      provides: "Migrated ai-agent with carousel.ts integration"
      contains: "carousel_text"
    - path: "supabase/functions/whatsapp-webhook/index.ts"
      provides: "Migrated webhook"
      contains: "createServiceClient"
  key_links:
    - from: "supabase/functions/ai-agent/index.ts"
      to: "supabase/functions/_shared/carousel.ts"
      via: "import { generateCarouselCopies, cleanProductTitle }"
      pattern: "import.*carousel"
    - from: "supabase/functions/ai-agent/index.ts"
      to: "agent.carousel_text"
      via: "agent.carousel_text || 'Confira:'"
      pattern: "carousel_text.*Confira"
---

<objective>
Migrate the 4 largest edge functions (426-1898 LOC) to shared utilities AND integrate carousel.ts into ai-agent (per D-03, D-04). This is the final migration plan — after this, zero inline createClient or raw Response calls remain across all 28 functions.

Purpose: Complete the full migration and eliminate the last carousel duplication + configurable text.
Output: 4 updated large function files. Phase 7 fully complete.
</objective>

<execution_context>
@C:/Users/georg/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/georg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-consolidacao-de-helpers-e-reducao-de-duplicacao-backend/07-CONTEXT.md
@.planning/phases/07-consolidacao-de-helpers-e-reducao-de-duplicacao-backend/07-RESEARCH.md
@.planning/phases/07-consolidacao-de-helpers-e-reducao-de-duplicacao-backend/07-01-SUMMARY.md

<interfaces>
From supabase/functions/_shared/supabaseClient.ts:
```typescript
export function createServiceClient(): SupabaseClient
export function createUserClient(req: Request): SupabaseClient
```

From supabase/functions/_shared/carousel.ts:
```typescript
export function cleanProductTitle(title: string): string
export async function generateCarouselCopies(product: any, numCards: number): Promise<string[]>
export function parseCopyResponse(text: string, count: number): string[] | null
export const CAROUSEL_CACHE_TTL_MS: number
export const CAROUSEL_CACHE_MAX_SIZE: number
```

From supabase/functions/_shared/response.ts:
```typescript
export function successResponse<T>(corsHeaders: CorsHeaders, data: T, status?: number): Response
export function errorResponse(corsHeaders: CorsHeaders, error: string, status?: number, details?: string): Response
```

From supabase/functions/_shared/logger.ts:
```typescript
export function createLogger(functionName: string, requestId?: string): Logger
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate transcribe-audio + whatsapp-webhook + uazapi-proxy (3 large functions)</name>
  <files>
    supabase/functions/transcribe-audio/index.ts
    supabase/functions/whatsapp-webhook/index.ts
    supabase/functions/uazapi-proxy/index.ts
  </files>
  <read_first>
    supabase/functions/transcribe-audio/index.ts
    supabase/functions/whatsapp-webhook/index.ts
    supabase/functions/uazapi-proxy/index.ts
    supabase/functions/_shared/supabaseClient.ts
    supabase/functions/_shared/response.ts
  </read_first>
  <action>
    Apply the 3-step mechanical transformation to 3 large functions:

    **transcribe-audio (426 LOC):**
    - Likely uses a dual pattern (user client for auth + service client for DB writes)
    - Replace inline clients with `createServiceClient()` / `createUserClient(req)` as appropriate
    - Replace all `new Response(JSON.stringify(...))` with `successResponse` / `errorResponse`
    - Replace all `console.log/error` with structured logger
    - This function handles audio processing — preserve any non-JSON responses

    **whatsapp-webhook (905 LOC):**
    - Module-level service client (no JWT — webhook from UAZAPI)
    - Replace `const supabase = createClient(SUPABASE_URL, SERVICE_ROLE_KEY)` with `const supabase = createServiceClient()`
    - Uses `webhookCorsHeaders` from cors.ts — pass those to `successResponse`/`errorResponse`
    - CRITICAL: This function has many console.log calls for message processing telemetry. Each must become structured `log.info/warn/error` with relevant data fields (messageId, contactPhone, messageType, etc.)
    - The webhook already uses `createLogger` — NO, verify by reading. If not, add it.
    - Preserve the fire-and-forget patterns (EdgeRuntime.waitUntil, fetchFireAndForget)
    - Keep early `return new Response('OK', { status: 200 })` for group messages and status updates as-is OR convert to `successResponse(webhookCorsHeaders, { status: 'ignored' })`

    **uazapi-proxy (939 LOC):**
    - Complex proxy function with many endpoints
    - Dual client pattern: user client for auth, service client for DB lookups
    - Has many different response paths (proxy responses, error responses, etc.)
    - Some responses may stream or forward raw UAZAPI responses — do NOT convert those to successResponse
    - Only convert the proxy's OWN JSON error/success responses
    - Has many console.log calls — convert all to structured logging
    - IMPORTANT: uazapi-proxy may forward UAZAPI API responses as-is. These must NOT be wrapped in successResponse. Only wrap responses that the proxy itself constructs.
  </action>
  <verify>
    <automated>cd C:/Projetos/Claude/WhatsPRO && npx vitest run --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - For each of the 3 files: grep -c "createClient.*esm.sh" returns 0
    - whatsapp-webhook: grep "createServiceClient" returns match, module-level instantiation preserved
    - uazapi-proxy: grep "createServiceClient\|createUserClient" returns matches
    - transcribe-audio: grep "createServiceClient\|createUserClient" returns matches
    - For each: grep "createLogger" returns match
    - whatsapp-webhook: early returns for groups/status preserved
    - uazapi-proxy: UAZAPI proxy-through responses NOT wrapped in successResponse
    - All tests pass
  </acceptance_criteria>
  <done>3 large functions fully migrated. whatsapp-webhook preserves fire-and-forget patterns. uazapi-proxy preserves proxy-through responses. All use shared utilities.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate ai-agent + integrate carousel.ts + configurable carousel_text (D-03, D-04)</name>
  <files>
    supabase/functions/ai-agent/index.ts
  </files>
  <read_first>
    supabase/functions/ai-agent/index.ts (full file — 1898 LOC)
    supabase/functions/_shared/carousel.ts
    supabase/functions/_shared/supabaseClient.ts
    supabase/functions/_shared/response.ts
  </read_first>
  <action>
    **1. Client migration (per D-01):**
    - Remove: `import { createClient } from 'https://esm.sh/@supabase/supabase-js@2'`
    - Remove: `const SUPABASE_URL = Deno.env.get('SUPABASE_URL')!` and `const SERVICE_ROLE_KEY = Deno.env.get('SUPABASE_SERVICE_ROLE_KEY')!`
    - Replace `const supabase = createClient(SUPABASE_URL, SERVICE_ROLE_KEY)` with `import { createServiceClient } from '../_shared/supabaseClient.ts'` + `const supabase = createServiceClient()`
    - Keep module-level instantiation (this is a background function).
    - Keep `GEMINI_API_KEY`, `GROQ_API_KEY`, `MISTRAL_API_KEY` env reads ONLY if they are still used directly in ai-agent after carousel extraction. If generateCarouselCopies was the only consumer of GROQ/MISTRAL keys, and those are now read inside carousel.ts, remove them from ai-agent. Check if any other code in ai-agent uses these keys directly (e.g., TTS uses GEMINI_API_KEY).

    **2. Carousel integration (per D-03):**
    - Remove from ai-agent: `cleanProductTitle` function (lines 20-36), `COPY_PROMPT` const (lines 39-47), `parseCopyResponse` function (lines 49-57), `_carouselCopyCache` + cache constants (lines 62-64), `generateCarouselCopies` function (lines 66-148) — total ~130 LOC removed
    - Add import: `import { generateCarouselCopies, cleanProductTitle } from '../_shared/carousel.ts'`
    - Verify all call sites still work: `generateCarouselCopies(p, photos.length)` at lines ~1083 and ~1171 should work unchanged (same function signature)
    - Verify `cleanProductTitle(title)` call sites still work (should be in card-1 generation inside carousel logic, now in carousel.ts)

    **3. Configurable carousel text (per D-04):**
    - Find all 4 occurrences of `'Confira:'` and replace with `agent.carousel_text || 'Confira:'`:
      - Line ~1101: `{ phone: contact.jid, message: agent.carousel_text || 'Confira:', carousel }`
      - Line ~1102: `{ number: contact.jid, text: agent.carousel_text || 'Confira:', carousel }`
      - Line ~1130: `content: agent.carousel_text || 'Confira:'`
      - Line ~1132: `media_url: JSON.stringify({ message: agent.carousel_text || 'Confira:', cards: carousel })`
    - NOTE: `agent` variable is already in scope at these locations (loaded from DB earlier in the handler). No new query needed.
    - NO SQL migration needed — agent config is flexible JSON.

    **4. Response migration (per D-02):**
    - ai-agent already imports `unauthorizedResponse` from auth.ts — keep that.
    - Add `import { successResponse, errorResponse } from '../_shared/response.ts'`
    - Replace remaining `new Response(JSON.stringify(...))` calls with `successResponse`/`errorResponse`
    - Keep OPTIONS preflight as-is.

    **5. Logger migration (per D-02):**
    - ai-agent already imports `createLogger` from logger.ts.
    - Find any remaining `console.log/error/warn` calls and replace with the existing `log` instance.
    - The agent has a `const log = createLogger('ai-agent', requestId)` inside the handler — use that for all request-scoped logs.
    - Module-level console calls (if any) get a module-level `const moduleLog = createLogger('ai-agent')`.

    **6. LLM metrics logging (per D-05):**
    - After each `callLLM()` call in the main LLM loop, add structured metric log:
      ```typescript
      const llmResult = await callLLM(req)
      log.info('LLM response', {
        provider: llmResult.provider,
        model: llmResult.model,
        latency_ms: llmResult.latency_ms,
        input_tokens: llmResult.inputTokens,
        output_tokens: llmResult.outputTokens,
        tool_calls: llmResult.toolCalls.length,
      })
      ```
    - This goes in the main agent loop where `callLLM` is invoked (likely 1-2 call sites).
    - Also add metric log for carousel copy generation: `log.info('Carousel copies generated', { provider, latency_ms })` — but this is inside carousel.ts now. Add a `log` parameter to `generateCarouselCopies` or have carousel.ts create its own logger. Simpler: carousel.ts creates `const log = createLogger('carousel')` at module level and logs internally.
  </action>
  <verify>
    <automated>cd C:/Projetos/Claude/WhatsPRO && npx vitest run --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "createClient.*esm.sh" supabase/functions/ai-agent/index.ts returns 0
    - grep "createServiceClient" supabase/functions/ai-agent/index.ts returns match
    - grep "import.*carousel" supabase/functions/ai-agent/index.ts returns match
    - grep -c "Confira:" supabase/functions/ai-agent/index.ts returns 0 (all replaced with carousel_text)
    - grep "carousel_text" supabase/functions/ai-agent/index.ts returns 4 matches (the 4 substitution sites)
    - grep -c "function generateCarouselCopies\|function cleanProductTitle\|function parseCopyResponse\|_carouselCopyCache\|COPY_PROMPT" supabase/functions/ai-agent/index.ts returns 0 (all moved to carousel.ts)
    - grep "latency_ms" supabase/functions/ai-agent/index.ts returns match (LLM metric logging)
    - All tests pass
  </acceptance_criteria>
  <done>ai-agent fully migrated: ~130 LOC carousel logic moved to carousel.ts, 4x 'Confira:' replaced with agent.carousel_text, shared utilities adopted, LLM metrics logged. Zero inline createClient. Zero raw Response. Zero console.log.</done>
</task>

</tasks>

<verification>
**Phase-wide final checks:**
- `grep -rc "createClient.*esm.sh" supabase/functions/*/index.ts` returns 0 across ALL 28 functions
- `grep -rc "from 'https://esm.sh/@supabase/supabase-js@2'" supabase/functions/*/index.ts` returns 0 (only _shared/supabaseClient.ts imports from esm.sh)
- `grep -c "Confira:" supabase/functions/ai-agent/index.ts` returns 0
- `grep -c "carousel_text" supabase/functions/ai-agent/index.ts` returns >= 4
- `grep "import.*carousel" supabase/functions/ai-agent/index.ts` returns match
- `grep "latency_ms" supabase/functions/_shared/llmProvider.ts` returns matches
- All tests pass: `npx vitest run`
</verification>

<success_criteria>
- 4 large function files updated
- ai-agent ~130 LOC reduced via carousel extraction
- 4x 'Confira:' replaced with configurable agent.carousel_text
- LLM metrics (latency_ms, tokens, provider) logged after each callLLM
- Zero inline createClient across ALL 28 edge functions
- Zero raw Response construction for JSON responses across ALL functions
- All tests green
</success_criteria>

<output>
After completion, create `.planning/phases/07-consolidacao-de-helpers-e-reducao-de-duplicacao-backend/07-04-SUMMARY.md`
</output>
