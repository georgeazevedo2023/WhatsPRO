---
phase: 07-consolidacao-de-helpers-e-reducao-de-duplicacao-backend
plan: 03
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - supabase/functions/process-follow-ups/index.ts
  - supabase/functions/summarize-conversation/index.ts
  - supabase/functions/scrape-products-batch/index.ts
  - supabase/functions/auto-summarize/index.ts
  - supabase/functions/analyze-summaries/index.ts
  - supabase/functions/ai-agent-playground/index.ts
  - supabase/functions/sync-conversations/index.ts
  - supabase/functions/process-scheduled-messages/index.ts
  - supabase/functions/e2e-scheduled/index.ts
  - supabase/functions/send-shift-report/index.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "10 medium functions use createServiceClient/createUserClient instead of inline createClient"
    - "10 medium functions use successResponse/errorResponse instead of new Response(JSON.stringify(...))"
    - "10 medium functions use createLogger instead of console.log/error"
    - "Zero behavior change — all response shapes and API contracts preserved"
  artifacts:
    - path: "supabase/functions/ai-agent-playground/index.ts"
      provides: "Migrated playground"
      contains: "createServiceClient"
    - path: "supabase/functions/sync-conversations/index.ts"
      provides: "Migrated sync-conversations"
      contains: "createUserClient"
  key_links:
    - from: "supabase/functions/*/index.ts (10 files)"
      to: "supabase/functions/_shared/supabaseClient.ts"
      via: "import { createServiceClient } or { createUserClient }"
      pattern: "import.*supabaseClient"
---

<objective>
Migrate 10 medium-sized edge functions (242-414 LOC) to use the centralized supabaseClient.ts, response.ts, and logger.ts shared utilities (per D-01, D-02). Same mechanical transformation pattern as Plan 02 but for more complex functions.

Purpose: Complete the migration of remaining non-large functions, covering ~35% of total function count.
Output: 10 updated function files, all using shared utilities.
</objective>

<execution_context>
@C:/Users/georg/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/georg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-consolidacao-de-helpers-e-reducao-de-duplicacao-backend/07-CONTEXT.md
@.planning/phases/07-consolidacao-de-helpers-e-reducao-de-duplicacao-backend/07-RESEARCH.md
@.planning/phases/07-consolidacao-de-helpers-e-reducao-de-duplicacao-backend/07-01-SUMMARY.md

<interfaces>
From supabase/functions/_shared/supabaseClient.ts:
```typescript
export function createServiceClient(): SupabaseClient
export function createUserClient(req: Request): SupabaseClient
```

From supabase/functions/_shared/response.ts:
```typescript
export function successResponse<T>(corsHeaders: CorsHeaders, data: T, status?: number): Response
export function errorResponse(corsHeaders: CorsHeaders, error: string, status?: number, details?: string): Response
```

From supabase/functions/_shared/logger.ts:
```typescript
export function createLogger(functionName: string, requestId?: string): Logger
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate 5 service-client functions (process-follow-ups, scrape-products-batch, auto-summarize, process-scheduled-messages, e2e-scheduled)</name>
  <files>
    supabase/functions/process-follow-ups/index.ts
    supabase/functions/scrape-products-batch/index.ts
    supabase/functions/auto-summarize/index.ts
    supabase/functions/process-scheduled-messages/index.ts
    supabase/functions/e2e-scheduled/index.ts
  </files>
  <read_first>
    supabase/functions/process-follow-ups/index.ts
    supabase/functions/scrape-products-batch/index.ts
    supabase/functions/auto-summarize/index.ts
    supabase/functions/process-scheduled-messages/index.ts
    supabase/functions/e2e-scheduled/index.ts
    supabase/functions/_shared/supabaseClient.ts
    supabase/functions/_shared/response.ts
  </read_first>
  <action>
    Apply the same 3-step mechanical transformation from Plan 02 to each function:

    **1. Client migration (per D-01):**
    - All 5 are module-level service client functions (cron/background workers).
    - Remove `import { createClient } from 'https://esm.sh/@supabase/supabase-js@2'`
    - Remove `SUPABASE_URL` and `SERVICE_ROLE_KEY` env reads (only if used exclusively for client creation)
    - Replace `const supabase = createClient(...)` with `import { createServiceClient } from '../_shared/supabaseClient.ts'` + `const supabase = createServiceClient()`
    - Keep module-level instantiation (not per-request — these are background workers).

    **2. Response migration (per D-02):**
    - Import `successResponse`, `errorResponse` from `../_shared/response.ts`
    - Replace all `new Response(JSON.stringify({...}), { status, headers })` with appropriate helper
    - Preserve OPTIONS preflight: `new Response(null, { headers: corsHeaders })` stays
    - IMPORTANT for cron functions: some return simple `{ ok: true }` or `{ processed: N }` — make sure the shape is `successResponse(corsHeaders, { processed: N })` which becomes `{ ok: true, processed: N }`

    **3. Logger migration (per D-02):**
    - Import `createLogger` from `../_shared/logger.ts`
    - Create module-level logger: `const log = createLogger('function-name')`
    - Replace all `console.log/error/warn` with `log.info/error/warn`
    - These background functions often have many console.log calls for progress tracking — convert all to structured log.info with data fields

    **Special cases:**
    - `process-scheduled-messages`: may iterate over messages with logging per message — use `log.info('Processing message', { id, scheduledAt })` pattern
    - `e2e-scheduled`: test runner may have complex logging — preserve test result details in structured fields
    - `scrape-products-batch`: may have per-URL progress logging — convert to structured `{ url, index, total }`
  </action>
  <verify>
    <automated>cd C:/Projetos/Claude/WhatsPRO && npx vitest run --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - For each of the 5 files: grep -c "createClient.*esm.sh" returns 0
    - For each of the 5 files: grep "createServiceClient" returns match
    - For each of the 5 files: grep "successResponse\|errorResponse" returns at least 1 match
    - For each of the 5 files: grep "createLogger" returns match
    - All tests pass
  </acceptance_criteria>
  <done>5 background/cron functions fully migrated. All use createServiceClient (module-level), response helpers, and structured logging.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate 5 mixed-client functions (summarize-conversation, analyze-summaries, ai-agent-playground, sync-conversations, send-shift-report)</name>
  <files>
    supabase/functions/summarize-conversation/index.ts
    supabase/functions/analyze-summaries/index.ts
    supabase/functions/ai-agent-playground/index.ts
    supabase/functions/sync-conversations/index.ts
    supabase/functions/send-shift-report/index.ts
  </files>
  <read_first>
    supabase/functions/summarize-conversation/index.ts
    supabase/functions/analyze-summaries/index.ts
    supabase/functions/ai-agent-playground/index.ts
    supabase/functions/sync-conversations/index.ts
    supabase/functions/send-shift-report/index.ts
    supabase/functions/_shared/supabaseClient.ts
    supabase/functions/_shared/response.ts
  </read_first>
  <action>
    Apply the 3-step transformation. These functions have more complex patterns:

    **Dual-client functions** (analyze-summaries, send-shift-report, sync-conversations, summarize-conversation):
    - These create both a service client (for DB writes) AND a user client (for auth verification).
    - Replace both: `import { createServiceClient, createUserClient } from '../_shared/supabaseClient.ts'`
    - Module-level service client: `const serviceSupabase = createServiceClient()`
    - Request-level user client inside handler: `const userSupabase = createUserClient(req)`
    - Verify which variable names each function uses (some use `supabase` vs `serviceSupabase` vs `adminClient`) and keep the same names for minimal diff.

    **ai-agent-playground**:
    - Super-admin only function (382 LOC) — uses `verifySuperAdmin` from auth.ts.
    - Likely has a service client for DB operations. Replace with `createServiceClient()`.
    - Has complex response structures for playground results — preserve the full shape.
    - May already have partial logger usage — check and complete.

    **Response + Logger migration**: Same pattern as other plans.

    **Special cases:**
    - `sync-conversations`: May make many UAZAPI calls in a loop — structured logging should include `{ instanceId, conversationIndex, total }` for each iteration.
    - `send-shift-report`: Constructs a WhatsApp message — response shape may include the report content.
    - `ai-agent-playground`: Returns test results with tool calls, model responses, etc. — complex response shape that must be preserved exactly.
  </action>
  <verify>
    <automated>cd C:/Projetos/Claude/WhatsPRO && npx vitest run --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - For each of the 5 files: grep -c "createClient.*esm.sh" returns 0
    - For dual-client files: grep "createServiceClient\|createUserClient" returns matches for both
    - For each of the 5 files: grep "successResponse\|errorResponse" returns at least 1 match
    - For each of the 5 files: grep "createLogger" returns match
    - ai-agent-playground response shape preserved (test results, tool calls, etc.)
    - All tests pass
  </acceptance_criteria>
  <done>5 mixed-client functions fully migrated. Dual-client pattern uses both factories. All response shapes preserved. All use structured logging.</done>
</task>

</tasks>

<verification>
- All 10 functions import from _shared/supabaseClient.ts
- All 10 functions import from _shared/response.ts
- All 10 functions use createLogger
- Zero `createClient` from esm.sh in any of the 10 files
- Zero `console.log/error/warn` in any of the 10 files
- All tests pass
</verification>

<success_criteria>
- 10 function files updated with shared utility imports
- Zero inline createClient calls
- Zero raw Response construction for JSON responses
- Zero console.log/error calls
- All tests green
</success_criteria>

<output>
After completion, create `.planning/phases/07-consolidacao-de-helpers-e-reducao-de-duplicacao-backend/07-03-SUMMARY.md`
</output>
