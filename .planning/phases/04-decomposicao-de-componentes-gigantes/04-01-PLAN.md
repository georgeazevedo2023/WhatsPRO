---
phase: 04-decomposicao-de-componentes-gigantes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/playground.ts
  - src/pages/dashboard/AIAgentPlayground.tsx
  - src/components/admin/ai-agent/playground/PlaygroundManualTab.tsx
  - src/components/admin/ai-agent/playground/PlaygroundScenariosTab.tsx
  - src/components/admin/ai-agent/playground/PlaygroundResultsTab.tsx
  - src/components/admin/ai-agent/playground/PlaygroundE2eTab.tsx
autonomous: true
requirements:
  - DT-08-playground
must_haves:
  truths:
    - "AIAgentPlayground.tsx has fewer than 300 lines of code"
    - "All 4 tabs (manual, scenarios, results, e2e) render identically to before"
    - "Types are importable from src/types/playground.ts"
    - "npx vitest run passes with zero regressions"
    - "npx tsc --noEmit compiles without errors"
  artifacts:
    - path: "src/types/playground.ts"
      provides: "All Playground types and constants"
      exports: ["AIAgent", "ToolCall", "ChatMessage", "PlaygroundResponse", "Overrides", "ScenarioCategory", "TestStep", "ExpectedOutcome", "TestScenario", "ScenarioRunResults", "ScenarioRun", "WatchSpeed", "TOOL_META", "ALL_TOOLS", "MODELS", "PERSONAS", "CATEGORY_META", "DIFFICULTY_COLORS", "TEST_SCENARIOS"]
    - path: "src/components/admin/ai-agent/playground/PlaygroundManualTab.tsx"
      provides: "Manual chat tab content"
      min_lines: 150
    - path: "src/components/admin/ai-agent/playground/PlaygroundScenariosTab.tsx"
      provides: "Scenarios tab content"
      min_lines: 200
    - path: "src/components/admin/ai-agent/playground/PlaygroundResultsTab.tsx"
      provides: "Results tab content"
      min_lines: 50
    - path: "src/components/admin/ai-agent/playground/PlaygroundE2eTab.tsx"
      provides: "E2E tab content"
      min_lines: 120
  key_links:
    - from: "src/pages/dashboard/AIAgentPlayground.tsx"
      to: "src/types/playground.ts"
      via: "import"
      pattern: "from '@/types/playground'"
    - from: "src/components/admin/ai-agent/playground/PlaygroundManualTab.tsx"
      to: "src/types/playground.ts"
      via: "import"
      pattern: "from '@/types/playground'"
    - from: "src/pages/dashboard/AIAgentPlayground.tsx"
      to: "PlaygroundManualTab"
      via: "JSX props"
      pattern: "<PlaygroundManualTab"
---

<objective>
Decompose AIAgentPlayground.tsx (1353 LOC) into an orchestrator (<300 LOC) plus 4 tab sub-components and a shared types file.

Purpose: Resolve DT-08 for the Playground — the largest component in the codebase is unmaintainable at 1353 lines. After this plan, the orchestrator manages state and delegates rendering to focused sub-components.

Output: 6 files (1 types, 4 sub-components, 1 slimmed orchestrator). Zero behavior change.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-decomposicao-de-componentes-gigantes/04-CONTEXT.md
@.planning/phases/04-decomposicao-de-componentes-gigantes/04-RESEARCH.md
@src/pages/dashboard/AIAgentPlayground.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract types, constants, and TEST_SCENARIOS to src/types/playground.ts</name>
  <files>src/types/playground.ts, src/pages/dashboard/AIAgentPlayground.tsx</files>
  <read_first>
    - src/pages/dashboard/AIAgentPlayground.tsx (lines 1-375 contain all types and constants)
    - src/types/index.ts (check existing types directory structure)
  </read_first>
  <action>
    Create `src/types/playground.ts` containing everything from AIAgentPlayground.tsx lines 31-375:

    1. **Types to move (lines 35-125):** `AIAgent`, `ToolCall`, `ChatMessage`, `PlaygroundResponse`, `Overrides`, `ScenarioCategory`, `TestStep`, `ExpectedOutcome`, `TestScenario`, `ScenarioRunResults`, `ScenarioRun`, `WatchSpeed`. All must be `export`ed.

    2. **Constants to move (lines 127-174):** `TOOL_META`, `ALL_TOOLS`, `MODELS`, `PERSONAS`, `CATEGORY_META`, `DIFFICULTY_COLORS`. All must be `export`ed.
       - `TOOL_META` references lucide-react icons (`ShoppingCart`, `Package`, `Image`, `Bookmark`, `Tag`, `Columns3`, `UserCog`, `PhoneForwarded`, `Wrench`). Import these at the top of playground.ts.
       - `CATEGORY_META` references additional icons (`MessageSquare`, `RotateCcw`, `AlertTriangle`, `Eye`, `Shield`, `Timer`, `Zap`, `Layers`, `Mic`, `Sparkles`). Import these too.
       - Type of TOOL_META uses `typeof Wrench` — import `Wrench` from lucide-react and use `typeof Wrench` in the Record type for both TOOL_META and CATEGORY_META.

    3. **TEST_SCENARIOS to move (lines 180-375):** The full `TEST_SCENARIOS: TestScenario[]` array. Export it. Per D-07, this lives alongside the types in playground.ts.

    4. **Update AIAgentPlayground.tsx:** Remove lines 31-375 (types, constants, TEST_SCENARIOS). Add import at the top:
       ```typescript
       import {
         type AIAgent, type ToolCall, type ChatMessage, type PlaygroundResponse,
         type Overrides, type ScenarioCategory, type TestScenario, type ScenarioRun,
         type ScenarioRunResults, type WatchSpeed,
         TOOL_META, ALL_TOOLS, MODELS, PERSONAS, CATEGORY_META, DIFFICULTY_COLORS,
         TEST_SCENARIOS,
       } from '@/types/playground';
       ```

    5. **Verify D-08 (test imports):** The 7 test files in `src/pages/dashboard/__tests__/Playground*.test.ts` do NOT import from AIAgentPlayground.tsx (confirmed by grep). No test file changes are needed. Document this finding.

    Per D-07, D-08, D-15.
  </action>
  <verify>
    <automated>cd C:/Projetos/Claude/WhatsPRO && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `src/types/playground.ts` exists and contains `export interface AIAgent`, `export interface ChatMessage`, `export const TOOL_META`, `export const TEST_SCENARIOS`
    - `AIAgentPlayground.tsx` contains `from '@/types/playground'`
    - `AIAgentPlayground.tsx` does NOT contain `interface AIAgent` or `const TOOL_META` or `const TEST_SCENARIOS`
    - `npx tsc --noEmit` exits with code 0
    - grep confirms zero test files import from AIAgentPlayground: `grep -r "AIAgentPlayground" src/pages/dashboard/__tests__/` returns empty
  </acceptance_criteria>
  <done>Types file created with all 12 types + 7 constants + TEST_SCENARIOS exported. Orchestrator imports from @/types/playground. TypeScript compiles. D-07, D-08, D-15 satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Extract 4 tab sub-components and slim orchestrator to less than 300 LOC</name>
  <files>
    src/components/admin/ai-agent/playground/PlaygroundManualTab.tsx,
    src/components/admin/ai-agent/playground/PlaygroundScenariosTab.tsx,
    src/components/admin/ai-agent/playground/PlaygroundResultsTab.tsx,
    src/components/admin/ai-agent/playground/PlaygroundE2eTab.tsx,
    src/pages/dashboard/AIAgentPlayground.tsx
  </files>
  <read_first>
    - src/pages/dashboard/AIAgentPlayground.tsx (full file after Task 1 changes — should be ~1000 LOC)
    - src/types/playground.ts (created in Task 1)
  </read_first>
  <action>
    Create directory `src/components/admin/ai-agent/playground/` and 4 sub-components. Per D-01, all state stays in the orchestrator. Per D-02, sub-components only have UI-local state (refs for scroll/focus). Per D-03, naming matches tabs. Per D-04, ToolInspector stays inside ManualTab. Per D-06, files go in the playground/ subpasta.

    **A) PlaygroundManualTab.tsx (~250-350 LOC)**

    Move into this component:
    - The `renderChatMessages()` function body (lines ~729-810 in original)
    - The `renderInputBar()` function body (lines ~812-821 in original)
    - The TabsContent value="manual" JSX (lines ~876-930 in original): overrides panel, stats bar, chat area, empty state with personas

    Props interface `PlaygroundManualTabProps`:
    ```typescript
    messages, sending, input, attachedImage, bufferMode, bufferSec, bufferCountdown,
    showOverrides, overrides, selectedAgent, totalTokens, avgLatency,
    onInputChange, onSend, onClear, onAttachImage, onBufferModeChange, onBufferSecChange,
    onOverridesChange, onShowOverridesToggle, onRateMessage, onReplayMessage, onRunPersona,
    onKeyDown, onExportConversation
    ```

    Per D-02 and research Pitfall 5: create `scrollRef` and `inputRef` INSIDE PlaygroundManualTab (they are UI-local scroll/focus refs, not domain state). The `fileInputRef` for image attachment also lives here.

    Auto-scroll effect: add `useEffect(() => { scrollRef.current && (scrollRef.current.scrollTop = scrollRef.current.scrollHeight); }, [messages]);` inside this component. Remove the equivalent useEffect from the orchestrator.

    Import TOOL_META, ALL_TOOLS, MODELS, PERSONAS, CATEGORY_META from '@/types/playground'. Import needed lucide icons directly.

    **B) PlaygroundScenariosTab.tsx (~230-280 LOC)**

    Move the TabsContent value="scenarios" JSX (lines ~933-1145 in original).

    Props interface `PlaygroundScenariosTabProps`:
    ```typescript
    filteredScenarios, selectedCategory, scenarioSearch, selectedScenario, scenarioRun,
    watchSpeed, messages, sending,
    onCategoryChange, onSearchChange, onSelectScenario, onRunScenario,
    onPause, onResume, onStop, onWatchSpeedChange, onClearMessages
    ```

    `filteredScenarios` is computed in the orchestrator and passed as prop (research recommendation — badges in TabsTrigger need count from orchestrator).

    Import CATEGORY_META, DIFFICULTY_COLORS, TOOL_META from '@/types/playground'.

    **C) PlaygroundResultsTab.tsx (~60-80 LOC)**

    Move the TabsContent value="results" JSX (lines ~1148-1210 in original).

    Props interface `PlaygroundResultsTabProps`:
    ```typescript
    runHistory: ScenarioRun[]
    ```

    Import CATEGORY_META, TOOL_META from '@/types/playground'.

    **D) PlaygroundE2eTab.tsx (~130-160 LOC)**

    Move the TabsContent value="e2e" JSX (lines ~1213-1346 in original).

    Props interface `PlaygroundE2eTabProps`:
    ```typescript
    e2eNumber, e2eRunning, e2eResults, e2eCurrentScenario, e2eLiveSteps,
    e2eSelectedScenario, filteredScenarios, selectedAgent,
    onNumberChange, onRunE2e, onSelectE2eScenario
    ```

    Import CATEGORY_META, DIFFICULTY_COLORS, TOOL_META from '@/types/playground'.

    **E) Slim the orchestrator AIAgentPlayground.tsx**

    After extracting the 4 tabs + render helpers, the orchestrator keeps:
    - Imports (from playground.ts, from sub-components, React, UI libs)
    - All useState and useRef declarations (per D-01)
    - fetchAgents, useEffect
    - sendToAgent, handleSend, handleClear, handleKeyDown (use refs)
    - rateMessage, replayMessage, replaySession, runPersona
    - runScenario, pauseScenario, resumeScenario, stopScenario, computeResults (use refs)
    - runE2eScenario (uses refs)
    - filteredScenarios useMemo, totalTokens, avgLatency, exportConversation
    - Guards (isSuperAdmin, loading, no agents)
    - Header JSX (agent selector, overrides toggle, export/replay/reset buttons)
    - Tabs shell with TabsList + TabsTrigger badges
    - 4x `<TabsContent>` each containing a single `<PlaygroundXxxTab ...props />` instantiation

    **Critical for <300 LOC target:** After removing types/constants (345 lines) + render helpers (93 lines) + 4 tab bodies (465 lines) = 903 lines removed from 1353 = ~450 remaining. To get under 300:
    - Move `exportConversation` logic inline or compress it (currently 15 lines)
    - Compress the header JSX (currently ~28 lines of buttons — use shorter line formatting)
    - Move the `computeResults` function to `src/types/playground.ts` as a pure function (it only uses data, no refs) — saves ~18 lines
    - Move the scroll useEffect into PlaygroundManualTab — saves 1 line
    - The `testGuardrail` function (line 549) is dead code (eslint-disable unused) — remove it

    If after all extractions the file is still between 280-300, that is acceptable. The key structural goal is that no rendering logic remains in the orchestrator.

    Per D-01, D-02, D-03, D-04, D-05, D-06, D-12.
  </action>
  <verify>
    <automated>cd C:/Projetos/Claude/WhatsPRO && npx vitest run --reporter=verbose 2>&1 | tail -20 && wc -l src/pages/dashboard/AIAgentPlayground.tsx && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `wc -l src/pages/dashboard/AIAgentPlayground.tsx` shows a number less than 300
    - `src/components/admin/ai-agent/playground/PlaygroundManualTab.tsx` exists and contains `export` and `PlaygroundManualTabProps`
    - `src/components/admin/ai-agent/playground/PlaygroundScenariosTab.tsx` exists and contains `export` and `PlaygroundScenariosTabProps`
    - `src/components/admin/ai-agent/playground/PlaygroundResultsTab.tsx` exists and contains `export`
    - `src/components/admin/ai-agent/playground/PlaygroundE2eTab.tsx` exists and contains `export`
    - AIAgentPlayground.tsx contains `<PlaygroundManualTab` and `<PlaygroundScenariosTab` and `<PlaygroundResultsTab` and `<PlaygroundE2eTab`
    - AIAgentPlayground.tsx does NOT contain `renderChatMessages` or `renderInputBar`
    - `npx vitest run` exits with code 0 (zero test failures)
    - `npx tsc --noEmit` exits with code 0
  </acceptance_criteria>
  <done>4 sub-components created in playground/ subpasta. Orchestrator is under 300 LOC with only state, handlers, header, and tab instantiation. All tests pass. TypeScript compiles. D-01 through D-06, D-12, D-14 satisfied.</done>
</task>

</tasks>

<verification>
1. `wc -l src/pages/dashboard/AIAgentPlayground.tsx` < 300 (D-12)
2. `npx vitest run` — all tests green, zero regressions (D-14)
3. `npx tsc --noEmit` — no type errors (D-15)
4. `grep -r "from '@/types/playground'" src/` shows imports from types file
5. `ls src/components/admin/ai-agent/playground/` shows 4 files
6. `grep "AIAgentPlayground" src/pages/dashboard/__tests__/` returns empty (D-08 confirmed)
</verification>

<success_criteria>
- AIAgentPlayground.tsx < 300 LOC (orchestrator only)
- 4 sub-components exist in src/components/admin/ai-agent/playground/
- Types exported from src/types/playground.ts
- npx vitest run passes
- npx tsc --noEmit passes
- Zero functional regressions
</success_criteria>

<output>
After completion, create `.planning/phases/04-decomposicao-de-componentes-gigantes/04-01-SUMMARY.md`
</output>
