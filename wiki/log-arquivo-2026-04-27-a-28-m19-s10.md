---
title: Log Arquivado — 2026-04-27 + 2026-04-28 (M19-S10 v1+v2+v3 + Deploy)
type: log-archive
period: 2026-04-27 a 2026-04-28
sources: [log.md (rotacionado em 2026-04-29)]
updated: 2026-04-29
---

# Log Arquivado — Sessões 2026-04-27 + 2026-04-28

> Entradas movidas de `log.md` em 2026-04-29 (rotina de rotação — Regra 16). Cobertura: M19-S10 v1 → v2 → v3 (UX redesign) + deploy 16 commits represados.

---

## 2026-04-28 (Deploy v2 + 16 commits represados → prod)

### Causa raiz

Ao validar v2 (M19-S10 Service Categories) em prod, descoberto que a tab "Qualificacao" não aparecia em `crm.wsmart.com.br`. Investigação revelou que **origin/master estava em `c68a95b` (auditoria helpdesk de 2026-04-14)** e local master estava em `859dd3a` — **16 commits acumulados não pushados há 13 dias**. Backend (migrations + edge functions) já estava aplicado em prod via Supabase MCP, mas frontend (Docker imagem) nunca foi rebuilt desde 04-14. Resultado: descompasso silencioso — schema novo no banco, UI velha no browser.

### Commits represados (master local → origin/master)

`36a681c chore(deps): playwright` · `69437d1 feat(helpdesk): permissões granulares de inbox` · `86566d0 docs: D21 + R73` · `67b51f3 chore(planning): M19 S8` · `7075b25 feat(monitoring): M19 S8 Camada 1 — DbSize` · `4e40dc3 S8 Camada 2 — alerts` · `8ce1fe3 S8 Camada 3 — retention policies` · `b244df4 docs: fim sessão` · `2be66f4 docs: 5 retention policies ON` · `a356576 feat(monitoring): M19 S8.1 — Backup JSONL` · `0dc99d6 docs: S8.1 shipped` · `cc3cf0c docs: D22-D25 + R74-R77` · `7883f17 docs: helpdesk-detalhado §1.26` · `ce4fafc docs(orchestrator): refatorar CLAUDE.md` · `4ebde32 docs: fim sessão 04-26` · `859dd3a feat(m19-s10): service categories v2`

### Operação realizada

1. `git stash -u` do trabalho v3 em `feat/qualif-ux-redesign` (não-destrutivo, recuperado no fim)
2. `git checkout master`
3. `git push origin master` → 16 commits subiram (`c68a95b..859dd3a`)
4. GitHub Action `deploy.yml` disparou automaticamente
5. Build success em **53s** (`gh run 25027833217`)
6. Imagem `ghcr.io/georgeazevedo2023/whatspro@sha256:2685a39d…` taggeada como `859dd3a` + `latest` em 2026-04-28T00:54:54Z
7. `git checkout feat/qualif-ux-redesign` + `git stash pop` → trabalho v3 restaurado

### Auditoria pós-build

| Check | Resultado |
|-------|-----------|
| Imagem em ghcr.io com `:latest` | ✅ sha `2685a39d…` |
| RPC `add_lead_score_event` | ✅ existe |
| Agentes em formato v2 | ✅ 1/1 (Eletropiso migrado v1→v2) |
| Tabela `lead_score_history` | ✅ |
| Tabela `db_retention_policies` (S8) | ✅ |
| Tabela `inbox_users` (helpdesk) | ✅ |

### Redeploy via Portainer service webhook

Hetzner/Portainer não pulla `:latest` auto. Solução: service webhook do Portainer (`https://app.wsmart.com.br/api/webhooks/34259f8a-9643-4963-90c4-bf2fed4cf786`). Disparado via `curl -X POST` → HTTP 204. Tempo total redeploy: ~45s.

### Validação prod pós-redeploy

| Check | Antes | Depois |
|-------|-------|--------|
| Bundle hash | `index-CTGISI-Z.js` | `index-DrJ0Utbx.js` ✅ |
| Tabs do agente | 8 (sem Qualificacao) | **9** ✅ |
| Agente Eletropiso schema | já v2 | OK ✅ |

### Deploy 2 — v3 UX redesign + fix score-cap

Após validação do deploy v2, usuário identificou 2 issues: (1) "ainda muito técnico" — redesign v3 não havia sido pushado; (2) banner vermelho "Corrija os erros" disparando indevidamente em score-cap proposital.

**Operação:** commit `adb2bda` (10 files, +1240/-179) → merge `--ff-only` → push → CI 46s → webhook → bundle prod `index-DrJ0Utbx.js` → `index-0egZ2ilZ.js`.

**Fix score-cap:** novo helper `stageHasBlockingError()` em `ServiceCategoriesConfig.tsx` linhas 335-344. Banner vermelho agora só dispara em erros REAIS (id duplicado, range inválido, overlap, phrasing vazio). scoreCap continua só renderizando aviso amber inline.

**Validação prod pós-redeploy 2 (Playwright):** 22 checks ✅ — toggle Iniciante/Avançado, "Categorias de atendimento", emoji "🔍 IA busca produto" / "👤 Chama vendedor humano", RadioGroup Leve/Médio/Importante, banner vermelho NÃO aparece (fix funcionou).

### Status final

✅ Em prod: tab Qualificacao + UX didática + score-cap como warning não-bloqueante. 17 commits totalmente deployed em ~3h.

### Lição (R80 — promovida para erros-e-licoes.md)

NUNCA acumular commits represados em local master por > 1 sprint — cria descompasso silencioso entre backend (deployado via MCP) e frontend (esperando git push).

---

## 2026-04-27 (M19-S10 v3 — Qualif UX Redesign — em andamento ao final da sessão, completado em 2026-04-28)

### Goal & contexto

Após audit pós-shipping da v2, identificado que a UX da tab "Qualificacao" usa muito jargão técnico (regex, slug, exit_action, score_value, priority, phrasing template). Inacessível para admins leigos. Branch `feat/qualif-ux-redesign` criada para refatorar UI sem tocar schema.

### Snapshot DB Eletropiso pré-execução

- **Categoria `tintas`** (interesse_match: `tinta|esmalte|verniz|impermeabilizante`)
  - Stage 1 `identificacao` (0-40, search_products): `tipo_tinta` (10), `ambiente` (15), `cor` (15)
  - Stage 2 `detalhamento` (40-70, enrichment): `acabamento` (20), `marca_preferida` (20)
  - Stage 3 `fechamento` (70-100, handoff): `quantidade` (15), `area` (15)
- **Categoria `impermeabilizantes`**: 2 stages (triagem + fechamento)
- **Default fallback**: 1 stage `qualificacao_basica` (especificacao + marca + quantidade)

Slugs imutáveis pós-refactor: `tintas`, `tipo_tinta`, `ambiente`, `cor`, `acabamento`, `marca_preferida`, `quantidade`, `area`, `impermeabilizantes`, `aplicacao`, `especificacao`. Auto-slugify nunca regrava esses no modo Iniciante.

### Plano

`.planning/phases/M19-S10-qualif-ux-redesign/PLAN.md` — 24 tasks, 15 critérios, audit GO_WITH_CAVEATS aplicado (6 ajustes integrados).

---

## 2026-04-27 (M19-S10 v2 — Stages + Score)

### Razão da evolução

v1 (mesma data, schema plano com `qualification_fields[]` + boolean `ask_pre_search`) foi superseded antes da UI integrar ao admin. Motivos: admin não-técnico (UI binária ruim de explicar; funil visual com stages comunica intenção); conexão com `lead_score_history` (M19 S2) — v2 persiste score progressivo em tempo real.

### 7 sub-decisões (D26.1 a D26.7)

| # | Sub-decisão | Justificativa |
|---|-------------|---------------|
| D26.1 | Score persistente em tag `lead_score:N` + `lead_score_history` | Conecta com M19 S2/S3 sem retrabalho |
| D26.2 | Score reseta apenas em `ia_cleared:` | Consistente com clear context |
| D26.3 | 1 categoria primária por conversa via tag `interesse:` | Evita múltiplos funis competindo |
| D26.4 | Score NUNCA visível ao lead | Métrica interna gestor |
| D26.5 | Nova tab "Qualificação" (9ª) | Stages complexos justificam tab dedicada |
| D26.6 | `exit_action`: search_products / enrichment / handoff / continue | Stage decide comportamento ao atingir teto |
| D26.7 | `score_value` por field, total 100 por categoria | Alinhado com NPS-like scoring |

### 5 fases (F1.5 + F2 v2 + F3 v2 + F4 v2 + F5)

- **F1.5** — Backend isolado: migration `20260427000002`, helper reescrito (`getCurrentStage`, `getNextField`, `calculateScoreDelta`), RPC `add_lead_score_event`, 40+ testes
- **F2 v2** — Admin UI v2: tipos v2 + `ServiceCategoriesConfig.tsx` reescrito (drag-drop, slider score, preview funil)
- **F3 v2** — Edits em `ai-agent/index.ts` (HIGH RISK, 4 edits cirúrgicos): import expandido, VALID_KEYS expandido, `buildEnrichmentInstructions` reescrita, hook de score em `set_tags` handler
- **F4 v2** — Doc + Seed: 6 wikis + PRD + log
- **F5** — Nova tab "Qualificação" (9ª) em `AIAgentTab.tsx`

### Migration v2 aplicada via Supabase MCP

- 1 agente em prod migrado v1→v2 (3 stages na categoria "tintas")
- 0 agentes ainda em formato v1
- Função `add_lead_score_event(_lead_id, _agent_id, _score_delta, _category_id?, _stage_id?, _field_key?, _conversation_id?)` criada
- **Bug corrigido antes de aplicar:** R34 violado na migration original (`FROM public.leads`); corrigido para ler `conversations.tags` via `_conversation_id` parâmetro

### Validação pós-shipping

- `npx tsc --noEmit` → 0 erros
- `npx vitest run` → **526 passed**, 5 falhas pré-existentes (FormBuilder/useForms — sem relação)
- 0 regressões

### Status final M19-S10 v2

✅ Todas as 5 fases shipped. Service Categories com Stages + Score progressivo em produção. Admin acessa em `/dashboard/ai-agent` → tab **"Qualificacao"** (4ª tab, ícone ListTree).

---

## 2026-04-27 (M19-S10 v1 — Service Categories)

### Objetivo da fase

Substituir 4 regras hardcoded de qualificação no AI Agent ("QUALIFICAÇÃO DE TINTAS", "fosco ou brilho", `if (interesse.includes('tinta'))` em `buildEnrichmentInstructions`, system_prompt do template Home Center) por schema único editável `ai_agents.service_categories JSONB`. Habilita multi-tenant real.

### 3 agentes em paralelo

- **Agente A — F1 Backend** — migration `20260427000001` + helper `_shared/serviceCategories.ts` (`matchCategory`, `getQualificationFields`, `formatPhrasing`, `getCategoriesOrDefault`) + 14 testes
- **Agente B — F2 Admin UI** — tipos compartilhados + `ServiceCategoriesConfig.tsx` (editor visual com drag-drop, validação regex, preview)
- **Agente C — F4 Doc + Seed** — `nicheTemplates.ts` + 7 wikis + log + PRD changelog

### F3 — Substituição em ai-agent/index.ts (HIGH RISK)

5 edits cirúrgicos. 43 inserções, 23 deleções, 1 file changed.

1. Import do helper `_shared/serviceCategories.ts` (5 funções)
2. `buildEnrichmentInstructions` reescrita: lê `agent.service_categories` via helper, gera sugestões + exemplo de frase dinâmico baseado em `phrasing_enrichment` da categoria + lista `set_tags` keys reais
3. Chamada atualizada para passar `agent` como 5º arg
4. Linha 1167 — regra "QUALIFICAÇÃO DE TINTAS" substituída por "QUALIFICAÇÃO POR CATEGORIA"
5. Linha 1171 — regra "fosco ou brilho" hardcoded substituída por "siga as instruções exatas do [INTERNO]"

**Backward compat 100%:** `getCategoriesOrDefault(null|undefined)` retorna seed default que reproduz comportamento hardcoded original.

**Achado adicional não-tocado:** linha 1612 (PATH C — fallback "not well qualified") tem hardcode menor de fallback genérico. Decisão: deixar intacto, anotado em melhorias futuras.

### Validação F3

| Check | Resultado |
|-------|-----------|
| `npx tsc --noEmit` | 0 erros ✅ |
| Helper tests | 34/34 passam ✅ |
| Base completa | 470 passed (5 falhas pré-existentes sem relação) ✅ |
| HIGH RISK files intactos | ✅ (apenas `ai-agent/index.ts` modificado) |

### Status final M19-S10 v1

- ✅ F1 (Backend isolado): migration + helper + 34 testes
- ✅ F2 (Admin UI): tipos + ServiceCategoriesConfig + ALLOWED_FIELDS
- ✅ F3 (substituição em ai-agent/index.ts): 5 edits, backward compat 100%, 0 regressões
- ✅ F4 (Doc + seed): nicheTemplates + 7 wikis + PRD + log

### Pendências resolvidas

- ~~Aplicar migration via `mcp__supabase__apply_migration`~~ ✅ aplicada (versão `20260427000001`)
- ~~`npx supabase gen types`~~ ✅ types regenerados (4 linhas adicionadas: `service_categories: Json` em Row/Insert/Update + RPC `dispatch_backup_cleanup`)
